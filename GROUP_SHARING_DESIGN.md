# 그룹 시스템 설계 (가족 → 그룹 일반화)

**작성일** 2026-08-29 · **v4.0 적용** · **v4.1 개정**: 기도 제목 공유를 없애고 "찾은 하나님" 공유로 대체 · **v4.5 개정**: "그룹 현황" 탭을 "우리의 성경읽기"(그룹 진도판)로 재정의(`SOW_구조설계.md` 20절)

기존 "우리 가족"(고정된 하나의 가족 단위)을 없애고, **이메일로 초대해서 만드는 여러 개의 그룹**으로 일반화한다. 사용자는 여러 그룹에 동시에 속할 수 있고("우리 가족" 그룹 + "주일학교 5반" 그룹처럼), 그룹 안에서 서로의 기록을 선택적으로 공유한다.

## 0. v4.1 — 무엇을 공유하고 무엇을 공유하지 않는가

**기도 제목은 그룹에 공유하지 않는다.** 기도는 매우 개인적인 내용을 담기 쉽고, 그룹이 가족보다 커지면(교회 소그룹 등) 부담이 된다.

**대신 매일 필수로 쓰는 묵상 1번 질문("마음에 남은 말씀과 그 말씀에서 만난 하나님")의 답변만, 본인이 원할 때 그룹에 공유한다.** 이유:
- 이미 매일 쓰는 필수 질문이라 별도로 뭘 더 쓸 필요가 없다 — 공유는 그저 "이미 쓴 답변에 체크박스 하나"다.
- 기도 제목보다 훨씬 가볍다. "오늘 이 말씀에서 하나님을 이렇게 느꼈어요"는 부담 없이 나눌 수 있는 신앙 이야기지, 개인 사정이 아니다.
- 그룹 전체가 "오늘 다들 뭘 느꼈나"를 보는 게, "누가 무슨 기도 제목이 있나"를 보는 것보다 원칙 5("비교보다 기록")에도 더 잘 맞는다 — 서로의 답을 보는 게 자연스럽게 격려가 되지, 부담이 되지 않는다.

기도(4번 질문 "하나님께 기도해요")는 계속 완전히 개인 전용이다 — 아예 공유 토글 자체가 없다.

## 1. 왜 이렇게 바꾸나

- "가족"은 한 사람이 하나만 가질 수 있는 고정 단위라 확장성이 없었다. 교회 소그룹, 친구들과의 묵상 모임 같은 실제 사용 시나리오를 못 담았다.
- 이메일 초대는 SOW가 별도 서버 로직을 안 만들어도 **Supabase Auth의 이메일 매직링크**로 자연스럽게 구현 가능하다 — 초대 = 그 이메일로 가입 유도 + 그룹 멤버십 부여.

## 2. 핵심 설계 원칙 (필독)

1. **이메일을 입력한다고 바로 남의 기록이 보이면 안 된다.** 초대는 "수락 대기(pending)" 상태를 반드시 거친다.
2. **공유는 그룹 단위 자동 공개가 아니라, "찾은 하나님" 답변 하나하나 선택.** 묵상 1번 질문 답변 아래 "✨ 이 답변, 그룹에 공유하기" 체크박스로 그때그때 정한다 — 기본은 비공유. 기도(4번 질문)는 공유 기능 자체가 없다(0절 참고).
3. **그룹을 나가거나 삭제해도 그 전까지 공유했던 기록의 열람 기록 자체를 소급 삭제하진 않는다** — 다만 이후로는 더 이상 안 보인다. (일반적인 SaaS 관례, 사용자에게 이 정책을 명확히 안내해야 함)

## 3. Supabase 스키마 제안

```sql
-- 그룹
groups (
  id, name, owner_id, created_at
)

-- 그룹 멤버십 (초대 상태 포함)
group_members (
  id, group_id, user_id, email,
  status text check (status in ('pending','accepted')),
  role text check (role in ('owner','member')) default 'member',
  invited_at, accepted_at
)

-- 사용자가 여러 그룹을 오갈 때 "지금 보고 있는 그룹" 저장 (선택)
user_prefs (
  user_id, last_active_group_id
)

-- "찾은 하나님" 공유 — 묵상 1번 질문 답변 중 공유하기로 한 것만
group_shared_answers (
  id, group_id, author_id, book, step, prompt_id,
  text,             -- 1번 질문 답변 원문(공유 시점 스냅샷)
  created_at
)

-- 축복의 말 — 그룹 전체 또는 특정 멤버 대상
group_blessings (
  id, group_id, author_id, target_user_id, -- null이면 그룹 전체
  text, read_by jsonb, created_at
)
```

## 4. 초대 흐름

1. 그룹장이 "그룹 관리" 탭에서 이메일 입력 → `group_members`에 `status: 'pending'` 행 생성 + Supabase Auth의 매직링크 이메일 발송(초대 문구 커스터마이즈)
2. 초대받은 사람이 메일의 링크를 눌러 로그인/가입 → 앱이 자동으로 `status: 'accepted'`로 갱신
3. 수락 전까지 초대한 사람 화면엔 "초대 중" 뱃지만 보이고, 상대방 화면에는 아무것도 안 보인다(수락해야 비로소 그 그룹이 목록에 나타남)

## 5. 여러 그룹 오가는 UI

이미 있는 "코스 전환" 패턴(성경묵상 코스 선택 화면, `SOW_구조설계.md` 10절)을 거의 그대로 재사용한다 — 그룹도 결국 "여러 개 중 하나를 골라서 그 안의 내용을 본다"는 같은 구조이기 때문:

- "지금 그룹" 표시 + 그룹 전환 드롭다운(또는 카드 선택 화면)
- "+ 새 그룹 만들기" 버튼은 항상 노출
- 그룹이 하나도 없으면 "찾은 하나님"/축복의말 대신 "아직 속한 그룹이 없어요" 안내

## 6. 지금 프로토타입에 반영한 것

**v4.0**
- `content/meditation/elementary/family/` → `content/meditation/elementary/group/`로 폴더/내용 전면 개편
- 서브모듈에 **"⚙️ 그룹 관리"** 탭 신규 추가 — 그룹 만들기 폼, 이메일 초대 폼 UI까지는 그려지지만 **입력창은 비활성화(disabled)** 상태다.
- 축복의 말에 "받는 사람" 개념 추가(그룹 전체 또는 특정 멤버) — 이전엔 암묵적으로 "부모→자녀"였지만, 그룹은 구성원 관계가 다양해서 명시적으로 고르게 함

**v4.1**
- "🙏 기도정원" 탭을 없애고 **"💛 찾은 하나님"** 탭으로 교체
- 묵상 1번 질문(`verse-and-god`)에 `"shareable": true` 플래그 추가 — 콘텐츠 스키마에 이 필드가 있는 프롬프트에만 `shell.js`가 "✨ 이 답변, 그룹에 공유하기" 체크박스를 그려준다
- 체크박스 상태는 지금 `localStorage`(`sow.share.{key}`)에만 저장 — 실제로 다른 그룹원에게 보이려면 Supabase의 `group_shared_answers` 테이블 연동 필요

## 7. 다음 단계 (Supabase 연동 시)

- [ ] `groups`, `group_members`, `group_shared_answers`, `group_blessings` 테이블 실제 생성
- [ ] Supabase Auth 이메일 매직링크 설정 + 초대 메일 문구 커스터마이즈
- [ ] "그룹 관리" 탭의 입력창 실제 동작 연결(지금은 UI만 있고 disabled)
- [ ] 그룹 전환 UI를 코스 선택 화면 패턴으로 구현
- [ ] 체크박스로 표시한 "공유함" 상태를 실제로 `group_shared_answers`에 write — 지금은 로컬에만 저장됨
