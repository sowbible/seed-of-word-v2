# SOW 배포 가이드

**전제**: `seedofword.org` 도메인 보유 중, 기본 데이터가 이미 GitHub 저장소에 있음. 이 문서는 그 둘을 활용해 안정적으로 운영하는 방법을 정리한다.

## 왜 이 방법이 안정적인가

지금 SOW는 서버가 필요 없는 구조다 — 콘텐츠는 JSON 파일(git 버전관리), 셸은 정적 HTML/CSS/JS, 사용자 데이터(가족/기도/축복)만 Supabase를 쓴다(`SOW_구조설계.md` 원칙 2). 이런 구조에는 **GitHub Pages**가 가장 안정적이다:

- 배포 서버를 따로 운영/관리할 필요가 없다 (GitHub가 호스팅)
- `main` 브랜치에 병합되는 순간 자동 배포 — 사람이 손으로 업로드하다 실수할 여지가 없다
- 모든 콘텐츠 변경 이력이 git 커밋으로 남는다 — "누가 언제 무슨 걸음을 고쳤는지" 항상 추적 가능
- 무료이고, 트래픽이 늘어도 정적 파일이라 갑자기 느려지지 않는다

**대상 저장소**: `sowbible/seed-of-word-v2`

> 참고: `sowbible` 계정에는 `sowbible.github.io`라는 특수 저장소도 이미 있어요(계정 전체의 기본 GitHub Pages 사이트용). 지금은 건드리지 않고, `seed-of-word-v2`라는 **일반 프로젝트 저장소에 커스텀 도메인(seedofword.org)을 직접 연결**하는 방식으로 진행해요 — 프로젝트 저장소도 커스텀 도메인을 그대로 쓸 수 있어서 문제없습니다.

## 1. 저장소 정리 (한 번만)

1. 기존 GitHub 저장소를 그대로 쓴다 — 새로 만들지 않는다. 다만 8절/9절(구 진단 결과)에서 확인했듯, 레거시 브랜치(`sow/john/{1..42}/index.html` 통짜 페이지 구조)와 새 구조(`SOW_구조설계.md` v2.0)가 섞이지 않도록:
   - `main` 브랜치 = 새 구조(v2.0, 트랙 기반)만 유지
   - 레거시 코드는 `legacy-archive` 같은 별도 브랜치로 옮겨서 보관만 하고 더 이상 수정하지 않는다
2. 저장소 루트에 `CNAME` 파일 하나를 추가한다 (내용은 딱 한 줄):
   ```
   seedofword.org
   ```

## 2. GitHub Pages 켜기

1. 저장소 **Settings → Pages**
2. Source: `Deploy from a branch` → `main` 브랜치, 루트(`/`) 선택
3. 저장하면 `https://sowbible.github.io/seed-of-word-v2/` 으로 우선 배포됨 — 이때 정상적으로 뜨는지 먼저 확인

## 3. 도메인 연결 (seedofword.org 등록업체에서 설정)

도메인을 산 곳(가비아/후이즈/Route53 등)의 DNS 설정에 아래 레코드를 추가한다.

| 종류 | 호스트 | 값 |
|---|---|---|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |
| CNAME | `www` | `sowbible.github.io` |

적용까지 몇 시간 걸릴 수 있다. 이후 GitHub Pages 설정 화면에서 `seedofword.org`가 초록 체크(✅)로 인증되면 완료. **"Enforce HTTPS"** 체크박스를 꼭 켠다.

## 4. 콘텐츠만 바꿔 넣는 흐름 (중복 작업 방지)

목표: 콘텐츠 담당자는 `CONTENT_GUIDE.md` 템플릿만 채우고, JSON이나 배포는 신경 쓰지 않는다.

```
콘텐츠 담당자                 개발자 / 변환 도구              GitHub
─────────────                ──────────────────              ──────
CONTENT_GUIDE.md   ───────▶  템플릿 → JSON 변환    ───────▶  content/meditation/... 에 커밋
템플릿 채우기                (수작업 또는 스크립트)            └─ main에 push되는 순간
                                                                 GitHub Pages 자동 재배포
```

**지금 당장(수동)**: 채운 템플릿 텍스트를 개발자가 `CONTENT_GUIDE.md`의 스키마 그대로 JSON으로 옮겨 커밋. 걸음 하나당 파일 몇 개뿐이라 42걸음 전체를 수동으로 옮겨도 무리 없다.

**나중에(자동화, 다음 단계)**: `CONTENT_GUIDE.md` 템플릿 형식을 정확히 지키면, 간단한 변환 스크립트(예: Python)로 템플릿 파일들을 읽어 자동으로 해당 경로의 JSON을 생성/갱신하는 GitHub Action을 만들 수 있다. 이렇게 되면 콘텐츠 담당자가 템플릿 파일을 `content-drafts/` 같은 폴더에 올리고 PR을 여는 것만으로 콘텐츠가 반영된다. (`SOW_구조설계.md` 7절 다음 단계에 등록)

## 5. 안정성을 위한 최소 규칙

- **`main`에 직접 push하지 않고 Pull Request로 병합** — 특히 콘텐츠 JSON은 오타 하나로 앱이 깨질 수 있으니, 병합 전에 최소 한 번은 JSON 유효성 검사(`python3 -m json.tool 파일명`)를 거친다.
- 걸음 번호·경로 중복 방지: 새 걸음을 추가하기 전에 `CONTENT_GUIDE.md`의 42걸음 표에서 번호가 이미 쓰였는지 먼저 확인한다.
- Supabase(가족/기도/축복 데이터)는 이 정적 배포와 별개로 관리 — API 키 등 민감정보는 절대 저장소에 커밋하지 않고 환경변수로 분리한다.

## 6. 다음에 결정할 것

- [x] 저장소 확정: `sowbible/seed-of-word-v2`
- [ ] 변환 스크립트(템플릿 → JSON) 자동화 여부와 방식
- [ ] Supabase 프로젝트를 언제/어떻게 연결할지 (가족 모듈이 실제로 동작하려면 필요)
- [ ] 도메인 등록업체(가비아/후이즈 등) 확인 후 3절 DNS 레코드 실제 등록
