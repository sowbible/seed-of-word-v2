# 국어 확장 설계 — 한자 배우기 & 글쓰기·토론

**작성일** 2026-09-05 · **상태**: 설계만 완료, 아직 구현 전(확인 후 진행)

이 문서는 국어 모듈의 "한자" 탭과 "나눔" 탭을 확장하는 구조를 정리한 것이다. 확정된 4가지 결정사항을 반영했다:
1. 한자 → **한자 배우기**로 이름 변경, 획순 애니메이션 + 써보기 퀴즈 추가
2. 나눔 → **글쓰기 & 토론**으로 이름 변경
3. 그 장에서 뽑은 질문 3개는 **읽기만 하는 목록이 아니라, 눌러서 "선택됨" 표시**가 되는 방식
4. 획순은 **HanziWriter** 라이브러리 사용

---

## 1. 한자 배우기 탭

### 1-1. 화면 구성 (위→아래 순서)
```
🈶 오늘의 한자
┌─────────────────────────┐
│  言                       │  ← 지금 있는 카드 그대로
│  말씀 언                  │     (character/reading/meaningTags/
│  말씀 · 말 · 이야기        │      explanation/bibleNote)
│  "言"은 마음속 생각을...   │
│  📖 예수님은 하나님의...   │
└─────────────────────────┘

🖊️ 획순 보기
┌─────────────────────────┐
│   [HanziWriter 캔버스]    │  ← 획순 애니메이션 자동 재생
│   [↺ 다시보기]            │
└─────────────────────────┘

✍️ 써보기
┌─────────────────────────┐
│   [HanziWriter 퀴즈 캔버스]│  ← 사용자가 직접 획을 그음
│   (한 획씩 맞았는지 바로   │     틀리면 그 획만 다시 그리게 안내
│    표시됨 — 라이브러리 내장│
│    기능)                  │
│   [↺ 다시 쓰기]           │
└─────────────────────────┘
```

### 1-2. 콘텐츠 — 새 필드 필요 없음
`hanja.character`(예: "言") 하나만 있으면 획순/써보기 둘 다 자동으로 된다. HanziWriter가 그 글자의 획순 데이터를 CDN에서 그때그때 가져오기 때문이다(콘텐츠 작성자가 획순을 직접 입력할 필요 없음). 지금 있는 `content/korean/hanja/{책}/{장}.json` 스키마 그대로 재사용한다.

### 1-3. 기술 방식
```html
<script src="https://cdn.jsdelivr.net/npm/hanzi-writer@3/dist/hanzi-writer.min.js"></script>
```
```js
const writer = HanziWriter.create('character-target-div', '言', {
  width: 200, height: 200,
  showOutline: true,
  strokeAnimationSpeed: 1,
  delayBetweenStrokes: 300
});
writer.animateCharacter(); // "획순 보기"
// ---
writer.quiz({
  onComplete: (summary) => { /* "참 잘했어요" 정도만 — 점수 강조 없음, 27절 원칙과 동일 */ }
}); // "써보기"
```
- 라이브러리와 글자 데이터 둘 다 **인터넷에서 그때그때 불러온다**(CDN) — 오프라인에서는 안 됨. 지금 앱도 이미 Supabase, Google Fonts, godpia 등 외부 서비스에 의존하고 있어서 같은 성격의 의존성이다.
- 한자 데이터는 브라우저가 한 번 받으면 캐싱되니, 같은 글자를 여러 번 봐도 두 번째부터는 빠르다.

### 1-4. 톤 — "써보기" 결과도 점수 강조 안 함
27절에서 정한 원칙(점수/등수 없이 담백하게)을 여기도 그대로 적용한다. 다 쓰면 "참 잘 썼어요 🈶" 정도만 보여주고, 몇 획을 틀렸는지 숫자로 강조하지 않는다.

---

## 2. 글쓰기 & 토론 탭

### 2-1. 화면 구성
```
✏️ 글쓰기 & 토론

이 중에서 마음에 드는 질문을 하나 골라보세요
┌─────────────────────────┐
│ ○ 질문 1 텍스트           │  ← 누르면 선택됨(●)으로 바뀌고 배경 강조
│ ○ 질문 2 텍스트           │     (라디오 버튼처럼 하나만 선택 가능)
│ ○ 질문 3 텍스트           │
└─────────────────────────┘

[아주 넓은 글쓰기 칸 — 여러 줄, 지금보다 세로로 훨씬 큼]
[🎤 말하기 버튼 — 기존 음성인식 재사용]
```

### 2-2. "질문 선택"이 하는 일 — 딱 화면 강조만, 답은 하나
질문을 누르면 그 질문 항목이 하이라이트되고, 글쓰기 칸 위에 "선택한 질문: ○○○"처럼 다시 한번 보여줘서 뭘 쓰고 있는지 잊지 않게 한다. **답 입력칸은 질문 개수와 무관하게 하나뿐**이다 — 질문 3개용 텍스트박스 3개를 만드는 게 아니라, "지금 어떤 질문에 답하고 있는지"만 표시가 바뀌는 것.

이렇게 하는 이유: 아이가 세 질문 중 아무거나 골라서 자유롭게 답하면 되는 거지, 세 개를 다 채워야 하는 숙제가 아니기 때문이다(원문 요청: "원하는 거 하나만 쓰기").

### 2-3. 콘텐츠 스키마 (새로 추가)
```json
{
  "book": "john", "chapter": 1,
  "discussionQuestions": [
    { "text": "오늘 본문에서 하나님을 어떻게 만났나요?" },
    { "text": "이 말씀을 친구에게 어떻게 설명해줄 수 있을까요?" },
    { "text": "오늘 배운 것 중 제일 기억에 남는 건 뭔가요?" }
  ],
  "writingGuide": "위 질문 중 마음에 드는 것 하나를 골라 자유롭게 써보세요"
}
```
기존 `writingPrompt` 필드(제목+안내문구 하나) 대신, 질문 배열(`discussionQuestions`)로 바뀐다 — 스키마가 바뀌는 것이므로 기존 `content/korean/writing/john/1.json`도 이 형식으로 다시 채워야 한다(마이그레이션 필요, 27-2절의 낱말 퀴즈처럼 자동 변환은 안 됨 — 사람이 질문 3개를 새로 정해야 하는 콘텐츠라서).

### 2-4. 저장 키 — 질문별로 안 나눔
지금처럼 `korean:{책}:{장}:writing` 키 하나에 저장한다(질문마다 따로 안 나눔) — 답은 하나뿐이니 저장 위치도 하나면 충분하다. 이건 지금 "어휘 탭 밑에도 나눔 칸이 같이 뜨는" 구조(28-2절)에도 그대로 이어진다 — 어휘 탭에서 쓰든 글쓰기&토론 탭에서 쓰든 여전히 하나의 기록.

### 2-5. 글쓰기 칸 — 더 넓게
지금은 `rows="2"`(두 줄)인데, 여러 문장 쓰기엔 좁다. `rows="6"` 정도로 눈에 띄게 키우고, 최소 높이도 CSS로 넉넉하게 잡는다.

---

## 3. 확인해주실 것

1. **획순/써보기가 인터넷 연결에 의존한다는 점** 괜찮으신가요? (이미 Supabase, godpia 등도 그렇긴 해요)
2. **글쓰기&토론의 콘텐츠 스키마가 바뀐다는 점** — 지금 1장에 이미 채워둔 나눔 콘텐츠(`오늘 배운 말씀 나누기`)도 "질문 3개" 형식으로 다시 써야 해요. 1장 예시도 제가 새로 만들어드릴까요?
3. **국어라는 이름은 그대로 두는 걸로 할까요?** (제 추천대로)

이거 확인해주시면 바로 구현 들어갈게요.
