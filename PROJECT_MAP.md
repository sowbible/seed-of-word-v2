# 문서 지도 — 뭘 고치려면 어느 파일만 보면 되나

## 왜 이걸 만들었나

초반(v1.0~v1.4)에는 `SOW_구조설계.md` 한 파일에 설계·진단·구현 결과·가이드가 다 섞여 있어서, 뭔가 하나 바뀌면 문서 전체를 다시 훑고 다시 써야 했어요. 그래서 데이터/시간이 많이 들었죠. v2.0부터는 **역할별로 파일을 쪼개고, "문서 하나를 고칠 때도 바뀐 절(section)만 고친다"는 규칙**(`SOW_구조설계.md` 맨 위 "문서 관리 규칙")을 세웠어요. 지금은 아래처럼 **무엇을 고치고 싶은지에 따라 볼 파일이 정해져 있어요.**

## 파일별 역할 — 겹치지 않게 분리됨

| 이걸 고치고 싶다면 | 이 파일만 보면 됨 | 다른 파일은 안 건드림 |
|---|---|---|
| 모듈/트랙 구조, 폴더 구조, 설계 원칙 | `SOW_구조설계.md` | 콘텐츠 내용, 배포 방법은 그대로 |
| 실제 콘텐츠(걸음별 본문·질문·어휘 등) 채워넣기 | `CONTENT_GUIDE.md` | 구조 코드, 배포 방법은 안 바뀜 |
| 그룹(가족→그룹) 공유 기능이 어떻게 동작하는지 | `GROUP_SHARING_DESIGN.md` | 구조/배포는 그대로 |
| **Supabase 연동 실제로 하는 법(SQL 포함)** | `SUPABASE_SETUP.md` | 위와 동일 |
| GitHub 저장소에 어떻게 올리는지 | `GITHUB_CONNECT.md` | 콘텐츠, 구조는 그대로 |
| 도메인 연결, GitHub Pages 설정 | `DEPLOY.md` | 위와 동일 |
| Windows에서 로컬 미리보기 방법 | `WINDOWS_PREVIEW.md` | 위와 동일 |
| 과거에 뭘 진단했고 뭘 측정했는지(참고용, 지금 안 씀) | `HISTORY.md` | 지금 구조와 무관 — 안 봐도 됨 |
| 전체 훑어보기 / 처음 오는 사람 안내 | `README.md` | 요약만, 상세 내용은 위 파일들로 링크 |

**실제 콘텐츠 파일**(`content/` 폴더 안의 JSON들)은 문서가 아니라 데이터라서 이 지도와는 별개예요 — `CONTENT_GUIDE.md` 템플릿대로 채워지는 결과물이 여기 쌓입니다.

## 그래서 뭐가 달라졌나 — 예시

- "국어 어휘 문구를 하나 고치고 싶다" → `CONTENT_GUIDE.md`의 템플릿 B 부분, 그리고 실제 콘텐츠 JSON 파일 하나만 바뀜. `SOW_구조설계.md`나 `DEPLOY.md`는 안 건드림.
- "도메인을 바꾸고 싶다" → `DEPLOY.md`만 수정. 콘텐츠나 구조 문서는 그대로.
- "Windows 안내 문구를 더 쉽게 풀고 싶다" (방금 한 것) → `WINDOWS_PREVIEW.md`만 수정. 나머지 6개 파일은 전혀 안 건드림.

## 예외 — 여러 파일이 같이 바뀔 수밖에 없는 경우

**구조 자체가 바뀔 때만** 여러 파일이 같이 바뀌어요. 예를 들어 지난번 "국어/언어/지식도 트랙을 갖게 하자" 같은 결정은 폴더 경로 자체가 바뀌는 거라, `SOW_구조설계.md` + `CONTENT_GUIDE.md`(경로 표기) + `shell.js`(실제 코드)가 같이 바뀌었어요. 이건 자연스러운 거예요 — 뼈대가 바뀌면 뼈대를 참조하는 곳들도 같이 바뀌어야 하니까요. 대신 **콘텐츠 문구나 배포 방법처럼 뼈대와 무관한 부분은 앞으로도 딱 그 파일 하나만** 고치면 됩니다.

## 참고: zip을 매번 다시 드리는 것에 대해

지금처럼 실제 파일을 고칠 때는 매번 전체를 다시 압축해서 드리고 있어요 — 파일 하나만 바뀌어도 압축은 전체를 새로 묶어야 하거든요. 다만 **압축은 매번 다시 하더라도, 그 안의 각 파일 자체는 필요한 부분만 고쳐지고 있는 거예요** (이 문서에서 설명한 구조 덕분에). 순수하게 "질문에 답만 하면 되는" 경우(예: 지금 이 질문)는 파일을 안 건드리고 대화로만 답할 수도 있어요.
