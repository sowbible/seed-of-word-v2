# SOW (Seed of Word) 구조 설계

**버전** v7.1 · **최종 수정** 2026-09-05

## 문서 관리 규칙

- 이 문서를 수정할 때는 **전체를 다시 쓰지 않고, 바뀐 절(section)만 수정**한다. (v2.0은 구조 자체를 재편하는 예외적 개정 — 이번 한 번만 전체를 다시 썼고, 다음 개정부터는 이 규칙으로 돌아간다.)
- 절 번호(`## 2.`, `## 3.` 등)는 한 번 부여되면 재배치하지 않는다. 절이 추가되면 하위 번호(`2-1`)나 맨 뒤 번호를 쓴다.
- 새 결정사항이 생기면 아래 변경 이력에 한 줄 추가하고, 해당 절에만 반영한다.
- 각 절의 `{#anchor-id}`는 고정 식별자다.
- **지나간 진단·측정 기록(감사, 성능 수치 등)은 본문에 쌓지 않고 `HISTORY.md`로 옮긴다.** 이 문서는 "지금 유효한 구조"만 담는다.

## 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1.0–v1.3 | 2026-08-16 | 모듈 레지스트리·챕터 스키마 최초 설계, 음성 입력 컴포넌트, 페이지 셸 프로토타입 구현 (상세 → `HISTORY.md`) |
| v1.4 | 2026-08-19 | 레거시 코드 재검토 후 "우리 가족" 모듈(기도정원/축복의 말/가족 현황) 등록, "비교보다 기록" 원칙 추가 |
| v2.0 | 2026-08-19 | 메인 화면을 4개 최상위 모듈로 재편(성경묵상/국어/언어/성경관련 지식), 성경묵상 안에 트랙(초등학생/하루한장/1년일독…) 레이어 신설, 지금까지 만든 모든 콘텐츠(묵상+국어+언어+지식+가족)를 "성경묵상 › 초등학생" 트랙으로 이관, 문서를 간결화하고 과거 기록은 `HISTORY.md`로 분리 |
| v3.0 | 2026-08-19 | 트랙 구조를 4개 모듈 전체로 확장 — 국어/언어/성경관련 지식도 각자 자기 트랙(현재는 셋 다 "초등학생" 트랙 하나씩)을 갖는 동일한 패턴으로 통일. 성경묵상에만 종속돼 있던 국어/언어/지식 콘텐츠를 각 모듈의 `elementary` 트랙으로 독립시킴. "우리 가족"은 계속 `meditation/elementary/family`에 남음 |
| v3.1 | 2026-08-19 | `shell.js`를 새 트랙 구조에 맞게 재작성 — 4개 모듈 전부 실제로 동작 확인(로컬 서버로 fetch 경로 전체 검증). GitHub에 올리지 않아도 로컬에서 바로 미리보기 가능하도록 README에 방법 추가 |
| v3.2 | 2026-08-19 | "성경 보기" 위젯 추가 — 레거시의 66권 성경 라이브러리 + godpia 역본 연동(새번역/개역개정4판/쉬운성경)을 새 구조로 이관. 성경묵상 걸음 화면에 역본 선택 + godpia 열람 버튼 + "다른 범위 읽기"(66권 탐색) 추가. 원칙에 "성경 본문은 자체 호스팅하지 않고 외부 연동" 추가 |
| v3.3 | 2026-08-19 | 성경묵상 코스 선택 화면 추가 — 트랙 라벨을 "1~20절 코스/1장 코스/1년 1독 코스"로 재정의하고, 트랙이 2개 이상인 모듈은 진입 시 자동으로 코스 선택 카드 화면을 보여주도록 `shell.js` 일반화 |
| v3.4 | 2026-08-19 | 성경 보기 위젯에 자유 범위 입력 추가 · 걸음 이전/다음 이동 바 추가 · 입력 저장/초기화 기능 추가(`app/js/persist.js`) |
| v3.5 | 2026-08-19 | 단일 템플릿 페이지로 전환(`PROPOSAL_BEST_STRUCTURE.md` 제안 1 채택) — `sow/read/index.html` 하나로 통합, `?book=&step=&module=` 쿼리로 걸음/탭 결정 |
| v3.6 | 2026-08-19 | 국어/언어/성경관련 지식이 성경묵상 코스를 자동으로 따라가도록 변경(`PROPOSAL_BEST_STRUCTURE.md` 제안 2 채택) — `followsTrackOf: "meditation"`으로 전환, 콘텐츠 폴더 이동 없음 |
| v3.7 | 2026-08-27 | 디자인 토큰 정제 — 파인그린·세이지·클레이 팔레트, 원형 아이콘 배지를 시그니처 요소로, 카드 그림자 단계 추가. `app/css/shell.css` 파일 하나만 교체 |
| v3.8 | 2026-08-29 | 묵상 코스 재구성 — 순서/이름을 "자유코스 · 하루한장 코스 · 1년 1독 코스 · 어린이 1~20절 코스"로 변경, 자유코스 신규 추가(콘텐츠 없이 바로 사용 가능) |
| v3.9 | 2026-08-29 | 성경 보기 위젯 단순화(66권 그리드 피커 제거, 직접 입력으로 대체) · "오늘의 기록" 툴바 신규(📅 달력 · 💾 저장 · ↺ 새로고침) |
| v4.0 | 2026-08-29 | "우리 가족"을 "그룹"으로 일반화 — 고정 단일 가족 대신 이메일 초대로 만드는 여러 그룹. `content/meditation/elementary/family/` → `.../group/`로 전면 개편, "⚙️ 그룹 관리" 탭 신규. 상세 설계·스키마·초대 흐름은 신규 문서 `GROUP_SHARING_DESIGN.md` |
| v4.1 | 2026-08-29 | 기도 공유를 없애고 "찾은 하나님" 공유로 대체 — "🙏 기도정원" 탭 삭제, "💛 찾은 하나님" 탭 신규(묵상 1번 질문 답변만 선택 공유). 콘텐츠 스키마에 `"shareable": true` 플래그 추가 |
| v4.2 | 2026-08-29 | 하루한장 코스를 성경 전체(1189장)로 실제 생성 · 인라인 달력을 메인 화면에 항상 노출, 날짜 클릭 시 그 걸음으로 이동 |
| v4.3 | 2026-08-29 | 1년 1독 코스 완성 — 1189장을 365일로 정확히 균등 분배(하루 3~4장), 하루한장의 책/장 매핑 함수 재사용. `status: "planned"` → `"active"` |
| v4.4 | 2026-08-29 | "본문 바로 가기" 구약/신약 탭을 맨 앞으로 재배치, 공용 컴포넌트화해서 자유코스/하루한장/1년1독 세 코스 상단에 모두 추가 |
| v4.5 | 2026-08-29 | "나의 성경읽기"(달력+성경지도) / "우리의 성경읽기"(그룹 진도판) 신설 — 개인 기록 영역에 성경지도 뷰 추가, 그룹의 "그룹 현황" 탭을 "우리의 성경읽기"로 재정의 |
| v4.6 | 2026-08-29 | 신규 문서 `SUPABASE_SETUP.md` — Supabase 프로젝트 생성부터 실행 가능한 SQL까지 실전 가이드 |
| v5.0 | 2026-08-29 | Supabase 실제 연동 시작 — 프로젝트 생성 완료, 이메일 매직링크 로그인, `persist.js`가 로그인 상태면 `entries` 표에도 저장 |
| v5.1 | 2026-08-29 | basePath 자동 감지 + 저장소 루트 진입점 추가 — GitHub Pages 서브경로 배포 대응 |
| v5.2 | 2026-09-01 | 모바일 앱 느낌으로 전환 — PWA 적용(manifest.json, 자체 제작 아이콘), 모바일에서 하단 고정 탭바로 전환 |
| v5.3 | 2026-09-01 | **공식 로고로 아이콘 전체 교체** — 직접 그렸던 잎사귀 아이콘을 사용자님이 만들어주신 공식 SOW 로고(파랑/주황/빨강 "SOW" + 새싹 그래픽)로 교체. `favicon-16/32`, `apple-touch-icon`, `icon-192/512/1024`, 원본 `sow-logo-official.png` 반영. 루트 진입 페이지·README에도 로고 노출 |
| v5.4 | 2026-09-01 | 신규 문서 `NETLIFY_SETUP.md` — Private 저장소 유지하면서 무료 배포, HTTPS 자동 적용, 커밋마다 자동 재배포 |
| v5.5 | 2026-09-01 | "나의 성경읽기"(달력/성경지도)를 코스 선택 화면에도 노출 — 코스를 먼저 안 골라도 성경묵상 탭 진입 즉시 보임. Supabase 프로젝트 URL 오타 수정("Failed to fetch" 원인) |
| v5.6 | 2026-09-02 | 웹/모바일 레이아웃 분리 — 데스크톱(1024px+)에서 왼쪽 고정 사이드바로 전환, 본문 900px로 확장. CSS만으로 처리, HTML/JS 불변 |
| v5.7 | 2026-09-02 | 모바일/태블릿 하단 탭바 → 햄버거 드로어 메뉴로 전환, 로그인/달력/그룹을 nav 목록에 통합, 세리프 폰트 전체 제거(Pretendard 통일) |
| v5.8 | 2026-09-02 | "본문 바로 가기"를 접이식으로 전환 — 기본은 작은 버튼 하나, 눌러야 펼쳐짐 |
| v6.0 | 2026-09-02 | 메뉴 아코디언 + "나의 성경읽기" 전용 화면(오늘의 활동 요약 통계 카드 포함) |
| v6.1 | 2026-09-03 | 말씀묵상 화면 전체 글자 크기 키움(왼쪽 메뉴 수준 이상) — 본문 참조 12.5→14.5px, 걸음 제목 22→23px, 질문 제목 15.5→17px, 안내문구/입력창 13.5→15px, 아이콘 배지 36→40px |
| v6.2 | 2026-09-03 | 국어 서브탭을 어휘 / 한자 / 나눔(글쓰기) 3개로 재구성, 실제 콘텐츠로 채움 |
| v7.0 | 2026-09-03 | 국어/언어/성경관련 지식을 "트랙 무관" 구조로 전면 재설계, 왼쪽 메뉴에 서브탭 아코디언 추가 |
| v7.1 | 2026-09-05 | **어휘 콘텐츠 원칙 확정 + 낱말 퀴즈 추가** — 어휘는 "① 그 장 본문에 실제로 나온 단어 ② 아이들이 일상에서 잘 안 쓰는 낯선 말"이라는 두 기준을 정해서 1장 예시를 재작성(태초/독생자/권세/은혜/진리). "🎯 낱말 퀴즈로 복습하기" 신규 — **새 콘텐츠를 하나도 안 만들고** 오늘의 어휘 5개를 그대로 재활용해서 3지선다 퀴즈를 자동 생성(정답+같은 날 다른 단어 2개를 오답으로). 점수/등수 없이 그 자리에서 정답만 확인하는 가벼운 복습용 — "비교보다 기록"(원칙 5)과 같은 이유 |

---

## 1. 설계 원칙 {#principles}

1. **모든 것은 등록제** — 최상위 모듈도, 트랙(성경묵상 안의 초등학생/하루한장/1년일독)도, 서브모듈(어휘/한자/나눔 등)도 전부 "레지스트리에 등록된 항목"이다. 새로 추가 = 폴더 추가 + 레지스트리에 한 줄 등록. 코어 코드는 건드리지 않는다.
2. **콘텐츠는 파일, 사용자 데이터는 DB** — 성경 본문·묵상 질문·어휘·언어학습·탐험 정보는 JSON 파일(git 버전관리). 로그인/기록/그룹·기도·축복 데이터는 Supabase.
3. **콘텐츠는 "트랙 + 걸음"을 축으로 연결** — 4개 모듈 전부 동일한 패턴을 쓴다: 모듈 → 트랙(예: 초등학생) → `book + step`. 국어/언어/성경관련 지식이라고 해서 다른 구조를 쓰지 않는다 — 트랙이 하나뿐이라도 "트랙을 가진 모듈"로 취급한다. (v1.x의 `chapter` 개념을 `step`으로 명확히 함 — 실제 성경 장과 1:1로 대응하지 않는다.)
4. **인터페이스 언어 ≠ 학습 언어** — UI가 보이는 언어(locale)와 "언어" 모듈에서 배우는 언어(learningLanguage)는 완전히 분리된 설정이다.
5. **비교보다 기록** — 그룹·공동체가 함께 보는 화면(기도정원, 그룹 현황 등)은 순위나 완료 속도를 비교하지 않는다. "오늘 함께했다"는 사실을 담백하게 보여주는 것을 우선한다.
6. **성경 본문 전문은 자체 호스팅하지 않는다** — 새번역/개역개정/쉬운성경 등은 저작권이 있는 번역본이라, 우리 서버에 전문을 올리지 않고 godpia.com 같은 외부 공인 사이트로 "열람 연결"만 한다. 우리가 자체적으로 갖고 있는 건 어떤 책·장을 어떤 역본으로 열지 결정하는 매핑 정보(`content/bible/_library.json`, `_versions.json`)뿐이다.

---

## 2. 메인 화면 & 폴더 구조 {#folder-structure}

메인 화면은 **4개 최상위 모듈**이고, **네 개 모두 트랙 구조를 갖는다** — 지금은 넷 다 "초등학생" 트랙 하나씩만 활성화된 상태다.

| 순서 | 모듈 | 현재 활성 트랙 |
|---|---|---|
| 1 | 📖 **성경묵상** | 초등학생 (하루한장 / 1년일독은 예정) |
| 2 | 🔥 **국어** | 초등학생 |
| 3 | 🌍 **언어** | 초등학생 |
| 4 | 🎒 **성경관련 지식** | 초등학생 |

> **지금까지 만든 모든 것**은 4개 모듈 각각의 **"초등학생" 트랙**에 들어있다: 성경묵상(묵상 4문항 + 그룹), 국어(어휘/한자/나눔), 언어(영어/중국어/외국인을 위한 한국어), 성경관련 지식(시대상/지도/인물). "그룹"만 예외적으로 성경묵상 안에 남는다 — 기도·축복은 그 자체가 독립 모듈이라기보다 묵상 경험에 딸린 기능이기 때문이다.

```
/content
  /bible/
    _library.json                             # 66권 전체 목록 (구약/신약, 장 수, godpia 코드, 콘텐츠 준비 상태)
    _versions.json                            # 역본 3종 + godpia URL 규칙 (원칙 6)

  /_module-registry.json                     # 최상위 4개 모듈 목록 — 전부 hasTracks:true

  /meditation/                                # 1. 성경묵상
    /_track-registry.json                     # 트랙 목록 (초등학생/하루한장/1년일독…)
    /elementary/
      meta.json
      /steps/{book}/{n}.json                  # 묵상 본문 + 4개 질문
      /group/                                 # 기도정원/축복의 말/그룹 관리 — UI 문구만 파일, 실데이터는 DB (8절)
    /daily-chapter/                           # 트랙: 하루한장 (예정)
    /bible-in-a-year/                         # 트랙: 1년일독 (예정)

  /korean/                                    # 2. 국어
    /_registry.json                          # 서브모듈: 어휘/한자/나눔 (트랙 무관 — 25-4/26절)
    /{vocab,hanja,writing}/{book}/{chapter}.json   # 실제 성경 책/장 기준 (트랙id 레이어 없음)

  /world-languages/                           # 3. 언어
    /_registry.json                          # 서브모듈: 지원 언어 목록
    /{en,zh,ja,ko-for-foreigners}/{book}/{chapter}.json

  /explore/                                   # 4. 성경관련 지식
    /_registry.json                          # 서브모듈: 시대상/지도/인물
    /{era,map,people}/{book}/{chapter}.json

/i18n/ui/{locale}.json                        # 인터페이스 언어별 UI 문자열

/app                                          # 셸 코드 (css/js)
/sow/read/index.html                          # 모든 걸음이 함께 쓰는 단일 페이지 — ?book=&step=&module= 쿼리로 결정 (12절)
```

**성경묵상만 트랙마다 콘텐츠가 다르다**(트랙 = 코스 자체가 콘텐츠, 26절 참고). **국어/언어/성경관련 지식은 v7.0부터 트랙과 무관하다** — 성경묵상에서 어떤 코스를 고르든, 지금 실제로 읽고 있는 책/장이 같으면 국어 콘텐츠도 같다.

**책/장이 같으면 네 모듈 다 같은 날의 본문을 가리킨다** — 예: 모든 모듈의 `john/1.json`은 요한복음 1장과 연결된 콘텐츠다. 국어 어휘(요한복음 1장의 한자 "言")가 그날 묵상 본문(1:1 "태초에 말씀이 계시니라")과 짝을 이루는 것도 이 규칙 덕분이다.

---

## 3. 레지스트리 예시 {#registry-examples}

### 3-1. 최상위 모듈 `/content/_module-registry.json`
```json
{
  "modules": [
    { "id": "meditation", "order": 1, "label": { "ko": "성경묵상" }, "hasTracks": true, "trackRegistryPath": "content/meditation/_track-registry.json" },
    { "id": "korean", "order": 2, "label": { "ko": "국어" }, "hasTracks": true, "trackRegistryPath": "content/korean/_track-registry.json" },
    { "id": "world-languages", "order": 3, "label": { "ko": "언어" }, "hasTracks": true, "trackRegistryPath": "content/world-languages/_track-registry.json" },
    { "id": "explore", "order": 4, "label": { "ko": "성경관련 지식" }, "hasTracks": true, "trackRegistryPath": "content/explore/_track-registry.json" }
  ]
}
```
네 모듈 모두 `hasTracks: true` — 셸 입장에서는 예외 없이 동일한 방식으로 처리하면 된다.

### 3-2. 트랙 레지스트리 — 모든 모듈이 같은 형태
```json
// content/meditation/_track-registry.json
{ "tracks": [
  { "id": "elementary", "order": 1, "status": "active", "label": { "ko": "초등학생" }, "metaPath": "content/meditation/elementary/meta.json" },
  { "id": "daily-chapter", "order": 2, "status": "planned", "label": { "ko": "하루한장" } },
  { "id": "bible-in-a-year", "order": 3, "status": "planned", "label": { "ko": "1년일독" } }
]}

// content/korean/_track-registry.json  (world-languages, explore도 동일 패턴)
{ "tracks": [
  { "id": "elementary", "order": 1, "status": "active", "label": { "ko": "초등학생" }, "registryPath": "content/korean/elementary/_registry.json" }
]}
```
새 트랙은 이 배열에 한 줄 추가 + 해당 폴더 채우면 끝. `status: "planned"`인 트랙은 "준비 중"으로 흐리게 노출한다(7절 다음 단계). 국어/언어/성경관련 지식에 "중학생" 트랙을 새로 열 때도 정확히 같은 방식을 쓴다.

### 3-3. 서브모듈 예시 (트랙 안에서, 기존과 동일한 패턴)
각 모듈의 `elementary/_registry.json`은 v1.x와 동일한 `{ "submodules": [...] }` 패턴을 그대로 쓴다 (예: 국어의 어휘/한자/나눔, 언어의 지원 언어 목록, 성경관련 지식의 시대상/지도/인물).

---

## 4. 언어 설정 구조 {#language-settings}
```json
{
  "settings": {
    "interfaceLocale": "ko",
    "learningLanguage": "en"
  }
}
```
- `interfaceLocale` → `/i18n/ui/{locale}.json` 참조, 앱 전체 문자열에 적용
- `learningLanguage` → "언어" 모듈(또는 초등학생 트랙의 world-languages 동반 콘텐츠)에서 학습 중인 언어

---

## 5. 공통 입력 컴포넌트 — 음성 녹음 → 텍스트 입력 {#voice-input}

모든 "필수 기록" 텍스트 입력은 공통 컴포넌트 `VoiceTextInput`(`app/js/voice-text-input.js`, 구현 완료)을 재사용한다. 콘텐츠 JSON에는 로직 없이 설정만 얹는다:
```json
{ "input": { "type": "text", "voiceInput": true, "placeholder": "" } }
```
`voiceInput: true`면 녹음 버튼이 노출되고 STT 결과가 필드에 채워진다. 트랙이 늘어나도 이 컴포넌트는 그대로 재사용한다.

### 5-1. 입력 저장/초기화 — `app/js/persist.js`
"필수 기록" 입력창이 저장 없이 새로고침하면 날아가는 문제(초기 감사 요약에서 지적됨)를 해결하기 위해, `voice-text-input.js`와 같은 패턴(스캔 기반, 콘텐츠 스키마 변경 없음)으로 만들었다.
```html
<textarea data-persist-key="meditation:elementary:john:1:verse-and-god">
```
- `data-persist-key`가 붙은 필드는 자동으로 입력 0.5초 후 `localStorage`에 저장되고, "💾 저장됨 · HH:MM" 문구가 뜬다.
- 새로고침해도 값이 복원된다. "↺ 초기화" 버튼으로 지울 수 있다.
- 키 형식은 `{모듈}:{트랙}:{book}:{step}:{필드id}` — 걸음/트랙이 달라지면 자동으로 다른 저장 슬롯이 된다.
- **한계**: 지금은 브라우저 하나에만 남는 임시 저장소다. 다른 기기와 동기화되거나 서버에 남는 건 아니다 — 진짜 "기록"으로 남기려면 8절의 Supabase 연동이 필요하다(로그인 붙는 대로 이 로컬 저장값을 서버로 옮기는 마이그레이션이 다음 단계).

---

## 6. 걸음(Step) 데이터 스키마 {#step-schema}

트랙 내부 모든 콘텐츠(각 모듈의 트랙 안 데이터)는 다음 최소 키를 공유한다.
```json
{
  "trackId": "elementary",
  "book": "john",
  "step": 1,
  "version": "1.0"
}
```
`step`은 실제 성경 장(章)과 반드시 일치할 필요 없는 "며칠째 콘텐츠"라는 순번이다 (예: 초등학생 트랙 1걸음 = 요한복음 1:1–18, 실제 1장은 51절). 트랙마다 걸음의 길이·개수는 자유롭게 다르게 설계할 수 있다 — 예를 들어 "하루한장" 트랙은 걸음 = 실제 성경 한 장(章) 그대로가 될 수도 있다.

---

## 7. 다음 단계 {#next-steps}

- [x] `shell.js`를 트랙 인식형으로 개선 — 4개 모듈 모두 `content/{moduleId}/{trackId}/...` 경로로 fetch (v3.1, 로컬 서버로 동작 검증 완료)
- [x] 메인 메뉴 4개 각각에 트랙 "선택" UI 추가 — `shell.js`가 트랙 2개 이상인 모듈에서 자동으로 코스 선택 화면을 보여줌(v3.3). 지금은 성경묵상만 해당(국어/언어/지식은 아직 트랙 1개라 자동 진입)
- [ ] "하루한장", "1년일독" 트랙 콘텐츠 기획 (성경묵상)
- [ ] 국어/언어/성경관련 지식에 초등학생 외 다른 트랙(예: 중학생, 성인) 기획 여부 논의
- [ ] 그룹(기도정원/축복의 말/그룹 관리) Supabase 연동 — 지금은 `shell.js`에 탭과 정적 문구까지만 붙어있고 실제 데이터 저장은 안 됨. 스키마 제안은 `GROUP_SHARING_DESIGN.md` 참고
- [ ] 로그인/그룹 멤버십 연동(이메일 초대 포함), 요한복음 2~42걸음 콘텐츠 마이그레이션, 퀴즈 렌더러 — v1.x부터 이어지는 미완료 항목 (상세 → `HISTORY.md`)

---

## 8. 그룹 모듈 {#group-module}

**v4.0 개편**: "우리 가족"(고정 단일 단위)을 없애고 **그룹**(이메일 초대, 여러 개 가능)으로 일반화했다. 상세 설계·스키마·초대 흐름은 별도 문서 `GROUP_SHARING_DESIGN.md`에 정리했다 — 이 절에는 요약만 둔다.

성경묵상 › 초등학생 트랙 안에 `content/meditation/elementary/group/`로 존재한다. 국어/언어/성경관련 지식과 달리 독립 모듈로 승격하지 않은 이유: 나눔은 그 자체로 학습 콘텐츠가 아니라 묵상 경험에 딸린 기능이기 때문이다. 서브모듈: 🗺️ 우리의 성경읽기(v4.5, 그룹 진도판 — 20절) · 💛 찾은 하나님(v4.1, 묵상 1번 질문 답변 중 공유하기로 한 것만 모음) · 🙏 축복의 말 · ⚙️ 그룹 관리(그룹 만들기/이메일 초대, v4.0 신규). "비교 없이 기록만 쌓는다"는 원칙(1절 원칙 5)은 이 모든 서브모듈에 공통 적용된다.

`_registry.json`과 `_ui-copy.json`(버튼 라벨·빈 상태 문구)은 파일로 관리하지만, 실제 그룹·공유된 답변·축복 메시지·완료 기록은 원칙 2에 따라 Supabase에서 읽는다. 핵심 설계 원칙(자세한 근거는 `GROUP_SHARING_DESIGN.md` 참고):
- 이메일 초대는 반드시 "수락 대기" 상태를 거친다 — 입력만으로 바로 공유되지 않는다.
- **기도 제목은 공유하지 않는다.** 대신 묵상 1번 질문("마음에 남은 말씀과 그 말씀에서 만난 하나님") 답변만, 본인이 원하면 "✨ 이 답변, 그룹에 공유하기" 체크박스로 공유한다(글마다 선택, 기본은 비공유).

제안 스키마 요약(전체는 `GROUP_SHARING_DESIGN.md` 3절):
```sql
groups(id, name, owner_id, created_at)
group_members(id, group_id, user_id, email, status, role, invited_at, accepted_at)
group_shared_answers(id, group_id, author_id, book, step, prompt_id, text, created_at)
group_blessings(id, group_id, author_id, target_user_id, text, read_by jsonb, created_at)
```
확정 전 — 7절 다음 단계에서 로그인 설계와 함께 논의한다.

---

## 9. 성경 보기 위젯 {#bible-link-widget}

**작성일** 2026-08-19 · **v2 개정** 2026-08-29 · **근거** 레거시 코드(`sow-platform.js`)의 `BIBLE_LIBRARY`/`GODPIA_VOL`/역본 select 재검토

### 9-1. 무엇인가
성경묵상 걸음 화면 상단(본문 참조 바로 아래)에 붙는 위젯. 두 가지 기능:
1. **역본 선택 + godpia 열람** — 지금 보고 있는 걸음의 실제 책/장(예: 요한복음 1장)을 고른 역본(새번역/개역개정4판/쉬운성경)으로 godpia.com에 새 탭으로 연다.
2. **직접 범위 입력** — "로마서 3장"처럼 항상 보이는 입력창에 직접 타이핑해서 바로 godpia로 이동. 책 이름은 `_library.json`의 `shortKo`와 느슨하게 매칭(부분 일치)하고, 텍스트 안의 숫자를 장 번호로 읽는다. 책 이름을 못 찾으면 화면에 안내 문구만 뜨고 이동하지 않는다.

> **v2에서 뺀 것**: 구약/신약 탭 + 66권 그리드 + 장 드롭다운으로 고르던 "다른 범위" 패널을 없앴다. 클릭해서 여는 단계 없이 바로 타이핑하는 게 더 빠르고, 66권 중 대부분이 `coming`(준비중)이라 그리드 자체가 눌러도 대부분 비활성인 상태였다 — 실제로 쓸모 있는 건 직접 입력뿐이었다.

### 9-2. 왜 이렇게 설계했나
- **저작권** — 원칙 6에 따라 본문 전문을 직접 안 올리고 godpia로 연결만 한다.
- **어디에 붙일지** — 콘텐츠(국어/언어/지식)가 아니라 "지금 읽고 있는 본문 그 자체"와 관련된 기능이라, 국어·언어·지식 모듈이 아니라 **성경묵상의 걸음 화면에만** 둔다. 다른 모듈에서도 필요해지면 같은 위젯(`app/js/bible-link.js`)을 그대로 재사용하면 된다 — 모듈마다 새로 만들 필요 없음.
- **직접 입력이 걸음 이동이 아닌 이유** — 42걸음은 초등학생 트랙이 큐레이션한 고정 콘텐츠라, 임의의 책으로 "걸음 이동"을 시키면 트랙 구조가 깨진다. 그래서 직접 입력은 앱 내부 이동이 아니라 **godpia 열람 링크만 새로 여는 것**으로 한정했다.

### 9-3. 데이터 위치
```
content/bible/_library.json     # 66권: id, chapters(장 수), status(available|coming), shortKo, godpiaVol
content/bible/_versions.json    # 역본 3종 + godpia URL 조합 규칙
```
`status`는 지금 요한복음만 `available`, 나머지 65권은 `coming` — "하루한장"/"1년일독" 트랙에 실제 콘텐츠가 채워지는 대로 하나씩 `available`로 바꾸면 된다. (v2부터는 직접 입력만 있어서 `status`가 화면에 배지로는 안 보이지만, 데이터는 그대로 남겨뒀다 — 나중에 자동완성 등에 재사용 가능)

### 9-4. 걸음 콘텐츠 스키마 — 이미 준비돼 있었음
`content/meditation/elementary/steps/{book}/{n}.json`에 이미 `book`, `chapter`(실제 성경 장 번호) 필드가 있어서, 이 위젯은 별도 스키마 추가 없이 바로 연결했다. `CONTENT_GUIDE.md` 템플릿 A에도 이미 반영되어 있다.

---

## 10. 코스 선택 화면 {#course-selector}

**작성일** 2026-08-19

### 10-1. 무엇인가
성경묵상에 진입하면, 걸음 화면으로 바로 들어가지 않고 **먼저 코스를 고르는 카드 화면**이 뜬다. (v3.6부터 코스 선택 화면은 성경묵상에만 있다 — 13절 참고)

| 코스 id | 이름 | 대상/분량 | 상태 |
|---|---|---|---|
| `free` | 자유코스 | 원하는 본문 직접 선택 · 걸음 개념 없음 | ✅ 콘텐츠 불필요, 바로 사용 가능 (15절) |
| `daily-chapter` | 하루한장 코스 | 성경 전체 1189장, 창세기 1장부터 순서대로 | ✅ 콘텐츠 자동 생성, 바로 사용 가능 (17절) |
| `bible-in-a-year` | 1년 1독 코스 | 365일, 하루 3~4장씩 | ✅ 콘텐츠 자동 생성, 바로 사용 가능 (18절) |
| `elementary` | 어린이 1~20절 코스 | 초등학생 · 짧은 본문(5~15절) | ✅ 콘텐츠 있음 |

### 10-2. 왜 "트랙"이 아니라 "코스"라고 부르나
내부 데이터 구조(폴더명, id)는 계속 `track`을 쓴다. 다만 **사람에게 보이는 화면 문구는 "코스"** 로 쓴다(`_track-registry.json`의 `label`) — "1~20절 코스"처럼 실제로 사용자가 체감하는 분량 기준 이름이 트랙 id(`elementary`)보다 이해하기 쉽기 때문이다.

### 10-3. 동작 규칙
`shell.js`는 트랙 레지스트리(`hasTracks: true`)를 가진 모듈에서 다음 규칙을 따른다:
- 트랙이 **1개**면 → 자동 선택, 선택 화면 없이 바로 콘텐츠로 진입
- 트랙이 **2개 이상**이면 → 진입 시 코스 선택 카드 화면부터 보여줌 (지금의 성경묵상)
- 선택한 코스는 `localStorage`에 저장돼서 다음에 그 모듈에 들어오면 기억하고 있다가 바로 그 코스로 진입한다. 화면 상단 "↺ 코스 변경" 버튼으로 언제든 다시 고를 수 있다.
- `status: "planned"`인 코스를 고르면, 걸음 콘텐츠 대신 그 코스의 `meta.json` 설명을 보여주는 "준비중" 화면으로 대체된다 — 콘텐츠가 채워지면 레지스트리의 `status`만 `"active"`로 바꾸면 자동으로 정상 화면이 나온다.

국어/언어/성경관련 지식은 `hasTracks`가 아니라 `followsTrackOf`를 쓰기 때문에 이 규칙과 무관하다 — 13절 참고.

### 10-4. 데이터 위치
```
content/meditation/_track-registry.json    # 코스 카드에 필요한 icon/label/subtitle/description
content/meditation/{trackId}/meta.json     # 각 코스의 "준비중" 화면에 쓰이는 상세 설명
```

---

## 11. 걸음 이전/다음 이동 바 {#step-nav-bar}

**작성일** 2026-08-19

코스(트랙)가 활성 상태(`status:"active"`)면, 어느 모듈 탭(성경묵상/국어/언어/성경관련 지식)에 있든 상단에 "◀ 이전 걸음 · N/총 걸음 · 다음 걸음 ▶" 바가 뜬다. 걸음 이동은 페이지 전체를 다시 불러오는 방식(주소가 바뀜)이라, 지금 보고 있던 탭이 주소의 `module=` 파라미터에 함께 실려서 이동한다 — 그래서 국어 탭을 보다가 다음 걸음으로 넘어가도 국어 탭이 그대로 유지된다(12절 참고). 상단 탭을 직접 클릭할 때도 주소가 같이 갱신되어서, 그 상태에서 새로고침해도 탭이 유지된다.

- 총 걸음 수는 `content/meditation/{trackId}/meta.json`의 `totalSteps`를 기준으로 경계를 계산한다(1걸음 미만/총 걸음 초과는 버튼 비활성화).
- 페이지는 걸음마다 따로 만들지 않는다(12절). 콘텐츠가 아직 없는 걸음으로 이동해도 페이지는 정상적으로 뜨고, 대신 "이 걸음은 아직 준비되지 않았어요" 안내 카드가 콘텐츠 자리에 뜬다.

---

## 12. 단일 템플릿 페이지 {#single-template-page}

**작성일** 2026-08-19 · **근거** `PROPOSAL_BEST_STRUCTURE.md` 제안 1 채택

### 12-1. 무엇이 바뀌었나
v3.4까지는 걸음마다 실제 HTML 파일이 필요했다(`sow/john/1/index.html`, `sow/john/2/index.html`…). 이건 "페이지 셸이 챕터마다 복제되던 문제"를 고치겠다던 이 프로젝트의 원래 취지(8~9절)와 어긋나는 잔재였다 — CSS/JS는 공용화됐지만 HTML 파일 자체는 여전히 걸음 수만큼 필요했다.

v3.5부터는 **페이지가 `sow/read/index.html` 단 하나**다. 어떤 책·걸음·모듈 탭을 보여줄지는 주소의 쿼리 파라미터로 결정한다:
```
sow/read/index.html?book=john&step=5&module=korean
```
쿼리가 없으면 `book=john&step=1`이 기본값이라, `sow/read/index.html`만 열어도 1걸음이 보인다.

### 12-2. 무엇이 좋아졌나
- **새 걸음 추가 = 콘텐츠 JSON만 추가.** 페이지 파일을 안 만들어도 된다 — `CONTENT_GUIDE.md`의 "다음 단계"가 한 줄 짧아졌다.
- **이전/다음 걸음 이동에서 404가 원천적으로 안 난다.** 예전엔 다음 걸음 파일이 없으면 브라우저가 404 페이지를 띄웠지만, 이제는 페이지는 항상 뜨고 콘텐츠만 없을 뿐이다 — 11절의 `renderStepNotReady()`가 "아직 준비되지 않았어요" 카드로 부드럽게 처리한다.
- 지금 보던 모듈 탭(성경묵상/국어/언어/지식)이 걸음을 이동해도, 새로고침해도 유지된다(`module=` 파라미터).

### 12-3. 트레이드오프
주소가 `sow/john/5/`처럼 깔끔하지 않고 `sow/read/?book=john&step=5`처럼 물음표가 붙는다. 나중에 더 깔끔한 주소가 필요해지면(SEO, 공유 링크 등) 해시 라우팅(`#/john/5`)이나 서버 사이드 라우팅으로 바꿀 수 있지만, 지금 단계(로컬/GitHub Pages 정적 호스팅)에서는 쿼리 파라미터 방식이 가장 간단하고 안정적이다.

### 12-4. 콘텐츠 작성자에게 달라지는 것
없다. `CONTENT_GUIDE.md` 템플릿대로 JSON만 채우면 되고, 페이지 파일을 만들어달라고 개발자에게 요청할 필요가 아예 없어졌다.

---

## 13. 모듈 간 코스 종속 — `followsTrackOf` {#follows-track-of}

**작성일** 2026-08-19 · **근거** `PROPOSAL_BEST_STRUCTURE.md` 제안 2 채택

### 13-1. 무엇이 바뀌었나
v3.3~v3.5에서는 국어/언어/성경관련 지식도 성경묵상과 똑같이 "자기 트랙 레지스트리"를 가진 모듈이었다. 지금은 트랙이 하나뿐이라 문제가 안 보였지만, 다시 생각해보면 문제가 있었다: 성경묵상에 코스가 3개(1~20절/1장/1년1독)로 늘어나면, 국어도 이론상 코스마다 어휘를 따로 만들어야 하는 구조였다. "1년1독 코스의 국어 어휘"가 뭘 의미하는지도 애매했다 — 국어/언어/지식은 애초에 "오늘 이 본문과 짝지어진 보충 콘텐츠"일 뿐, 그 자체로 별도 코스를 가질 이유가 크지 않았다.

v3.6부터 국어/언어/성경관련 지식은 **자기 코스 레지스트리가 없다.** 대신 최상위 모듈 레지스트리에 `followsTrackOf: "meditation"`이라고만 적혀 있고, 이 세 모듈은 **성경묵상이 지금 고른 코스를 조용히 그대로 따라간다.**

### 13-2. 동작 규칙
- `hasTracks: true`인 모듈(성경묵상) → 10절 규칙대로 코스 선택 화면을 가진다.
- `followsTrackOf: "다른모듈id"`인 모듈(국어/언어/성경관련 지식) → 자기 코스 선택 화면이 **아예 없다.** 화면 상단에 "📖 지금 코스: 1~20절 코스 · 성경묵상 탭에서 바꿀 수 있어요" 배지만 조용히 표시된다. 코스를 바꾸려면 성경묵상 탭으로 가야 한다 — 국어 탭 안에서는 못 바꾼다.
- 성경묵상에서 아직 코스를 한 번도 고르지 않은 상태로 국어 탭에 먼저 들어와도, 선택 화면을 강제로 띄우지 않는다. 그냥 기본 코스(`status:"active"`인 코스)를 조용히 따라간다.

### 13-3. 왜 이렇게 했나
- **콘텐츠 제작 부담이 코스 하나(성경묵상)에만 집중된다** — 국어/언어/지식을 코스 수만큼 반복해서 만들 필요가 없다.
- **"국어에 왜 코스 선택이 있지?"** 하는 혼란이 없어진다.
- 데이터 폴더는 안 옮겼다 — 지금 유일한 코스 id가 `elementary`라서, `content/korean/elementary/...` 같은 기존 경로가 이미 정확히 맞았다. 바뀐 건 레지스트리(`_module-registry.json`)와 `shell.js`의 트랙 해석 로직뿐이다.

### 13-4. 나중에 정말 국어만의 독립 코스가 필요해지면
드물겠지만 "국어만 따로 초등/중등을 나누고 싶다" 같은 요구가 생기면, 그 모듈만 `followsTrackOf`를 지우고 다시 `hasTracks: true` + 자기 `_track-registry.json`으로 되돌리면 된다 — v3.5 이전 구조와 100% 동일한 패턴이라 언제든 되돌릴 수 있다.

---

## 14. 디자인 시스템 {#design-system}

**작성일** 2026-08-27

### 14-1. 팔레트
`app/css/shell.css`의 `:root` 토큰. 기존 변수명(`--forest`, `--rust`, `--gold`)은 새 이름의 별칭으로 남겨둬서 다른 파일을 안 건드려도 되게 했다.

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--ink` | `#1D3A2C` | 본문/헤딩 텍스트, 짙은 파인그린 |
| `--sprout` | `#78A768` | 포인트 컬러, 아이콘 배지, 성공/완료 |
| `--amber` | `#C89238` | 강조 텍스트(어휘 태그 등) |
| `--clay` | `#A85C3F` | 필수 표시, 경고, 성경 보기 CTA 계열 |
| `--paper` | `#F6F4EE` | 배경 |

**왜 이 팔레트인가**: 크림 배경 + 테라코타 포인트는 지금 AI 디자인에서 가장 흔한 조합이라(특히 `#D97757` 근처 색상), 그대로 쓰면 "AI가 만든 티"가 난다. 대신 브랜드명(Seed of Word)과 기도정원·새싹 모티프에 맞게 **파인그린을 메인, 세이지그린을 포인트**로 삼고, 필수/경고 색은 선명한 주황(테라코타) 대신 더 차분한 **클레이 톤**으로 눌렀다.

### 14-2. 시그니처 요소 — 원형 아이콘 배지
이모지가 텍스트 옆에 그냥 떠 있던 것을, **은은한 세이지톤 원 안에 담는 것**으로 통일했다(`.icon-row .emoji`, `.sow-track-icon`, `.sow-item-card > .e`, `.sow-word-chip > .e`). 카드 종류가 여러 개(묵상 질문/코스 카드/탐험 항목/어휘 칩)라도 아이콘이 항상 같은 원형 배지 안에 있어서 하나의 제품처럼 읽힌다. HTML/JS는 그대로다 — 기존에 이미 `class="emoji"`, `class="e"`처럼 클래스가 붙어 있어서 CSS만 바꿔도 적용됐다.

### 14-3. 그림자 단계
평면적인 흰 카드에 아주 옅은 그림자 3단계(`--shadow-sm/md/lift`)를 추가해 입체감을 줬다. 톤이 다 `rgba(29,58,44, ...)` — 검정이 아니라 잉크 컬러를 낮은 투명도로 써서, 그림자도 팔레트에 맞게 "초록빛이 도는" 그림자가 되도록 했다.

### 14-4. 바뀐 파일
`app/css/shell.css` 하나뿐이다. 콘텐츠 JSON, `shell.js`, `bible-link.js`, `persist.js`, `voice-text-input.js`는 전혀 안 건드렸다 — 클래스 이름이 전부 그대로라(58개 클래스 셀렉터 유지 확인) 디자인만 갈아 끼운 셈이다.

---

## 15. 자유코스 {#free-course}

**작성일** 2026-08-29

### 15-1. 무엇인가
미리 큐레이션된 본문이 없는 코스. 사용자가 그날그날 **책·장을 직접 골라서**, 다른 코스와 똑같은 4가지 질문(마음에 남은 말씀 / 오늘 살아갈래요 / 감사해요 / 기도해요)으로 묵상한다.

### 15-2. 왜 "콘텐츠 없이 바로 쓸 수 있는" 유일한 코스인가
다른 코스(어린이 1~20절, 하루한장, 1년1독)는 **매일 어떤 본문을 다룰지 사람이 미리 정해서** 걸음별 JSON을 만들어야 동작한다. 자유코스는 그 반대다 — 오늘 뭘 읽을지는 사용자가 그 자리에서 고르는 것이라, 애초에 "미리 만들어둘 콘텐츠"가 없다. 4문항 문구도 걸음마다 다르지 않고 항상 같다는 걸 이미 알고 있었기 때문에(`CONTENT_GUIDE.md` 템플릿 A의 안내), 그 문구를 `content/meditation/_prompt-template.json` 하나로 뽑아내 재사용했다.

### 15-3. 걸음(step) 개념이 없다 — `stepless: true`
트랙 레지스트리에 `"stepless": true`가 붙어 있으면 `shell.js`는:
- 11절의 "이전/다음 걸음" 바를 그리지 않는다 (애초에 순번이 없으므로)
- 묵상 화면 상단에 책·장을 고르는 셀렉트 2개(`.sow-free-picker`)를 대신 그린다
- 고른 책·장은 `localStorage`에 저장돼서 다음에 와도 기억한다
- 저장 키(`data-persist-key`)에도 걸음 번호 대신 고른 책·장을 그대로 써서(`meditation:free:{book}:{chapter}:{promptId}`) 다른 책을 고르면 자연스럽게 다른 기록 슬롯이 된다
- 성경 보기 위젯(9절)도 그대로 붙어서, 고른 본문을 바로 godpia로 열람할 수 있다

### 15-4. 국어/언어/성경관련 지식과의 관계
이 세 모듈은 13절에 따라 성경묵상의 코스를 그대로 따라간다. 사용자가 자유코스를 고른 상태로 국어 탭에 가면, `content/korean/free/...` 콘텐츠가 없어서 "이 걸음은 아직 준비되지 않았어요" 화면이 뜬다 — 자유코스는 애초에 성경묵상 전용 개념이라 자연스러운 동작이다.

### 15-5. 데이터 위치
```
content/meditation/_prompt-template.json   # 공용 4문항 (걸음/코스 무관)
content/bible/_library.json                # 책 선택 드롭다운에 재사용 (9절과 동일 데이터)
```
별도의 `content/meditation/free/` 폴더나 `meta.json`이 필요 없다 — 채울 콘텐츠 자체가 없기 때문이다.

---

## 16. 나의 성경읽기 — 달력 & 성경지도 {#session-toolbar}

**작성일** 2026-08-19 · **v3 개정** 2026-08-29 · **파일** `app/js/session-toolbar.js`, `app/js/reading-map.js`(신규)

### 16-1. 무엇인가
성경묵상 화면(어떤 코스든) 맨 위, **"📖 나의 성경읽기"** 라는 이름으로 묶여 있는 개인 기록 영역. **💾 저장 · ↺ 새로고침** 버튼과, 그 아래 **📅 달력 / 🗺️ 성경지도** 두 뷰를 탭으로 전환해서 본다.

> **v2에서 바뀐 것**: "📅 달력" 버튼을 눌러야 모달로 뜨는 구조였다가, 화면에 항상 그려두는 인라인 카드로 바뀌었고 날짜 클릭 시 그 걸음으로 이동하게 됐다.
> **v3에서 바뀐 것**: "나의 성경읽기"라는 이름 아래 **성경지도**가 추가됐다(16-2-2) — 실제로 읽은 성경 장(章)에 도장을 찍듯 표시하는 지도. 20절의 "우리의 성경읽기"(그룹 진도판)와 짝을 이루는 "나 혼자 보는 버전"이다.

### 16-2-1. 달력 — 활동 로그를 읽어서 보여주고, 클릭하면 그 걸음으로 이동
`persist.js`가 입력을 저장할 때마다(자동저장이든 "저장" 버튼이든) `localStorage`의 `sow.activity.dates`에 오늘 날짜를 기록한다. 이때 **저장 키(`data-persist-key`)를 파싱해서 어떤 모듈·코스·책·장이었는지까지 같이 남긴다**:
```js
// key 형식: "{moduleId}:{trackId}:{book}:{step}:{fieldId}"
if(parts[0] === 'meditation'){
  window.SOWActivityLog?.markToday({ trackId: parts[1], book: parts[2], step: parts[3] });
}
```
그래서 성경묵상 기록이 있는 날은 달력 칸에 `{book, step, trackId}`가 통째로 남고, 그 날짜를 클릭하면:
1. 저장된 `trackId`를 `sow.track.meditation`에 세팅 (자유코스였다면 `sow.free.passage`도 같이 복원)
2. `sow/read/index.html?book=...&step=...&module=meditation`으로 이동

국어 등 성경묵상이 아닌 모듈 기록은 `moduleId !== 'meditation'`이라 상세 정보 없이 그냥 "이 날 뭔가 했다"로만 남고, 그 날짜는 클릭해도 이동하지 않는다(CSS `.sow-cal-cell.clickable` 클래스가 안 붙음).

### 16-2-2. 성경지도 — 실제로 읽은 장에 도장 표시
달력이 "어떤 날 했는지"를 보여준다면, 지도는 **"성경 어디를 읽었는지"**를 보여준다. 66권 전체를 구약/신약으로 나눠 나열하고, 책마다 장 수만큼 작은 사각형을 그린다 — 읽은 장은 세이지 그린으로 채워진다("도장"). 총 읽은 장 수도 상단에 요약해서 보여준다.

**어떻게 "실제 장"을 정확히 아는가** — 걸음(STEP)이 항상 실제 성경 장과 같지는 않다(예: 어린이 코스는 걸음 번호 ≠ 실제 장). 그래서 달력과는 다른 방식을 쓴다:
1. 각 묵상 렌더러가 화면을 그릴 때, 그 화면이 다루는 **진짜 책/장**을 `window.SOWReadingMap.setCurrentChapters()`로 등록해둔다 (예: 1년1독처럼 하루에 여러 장을 다루면 배열로 등록).
2. `persist.js`가 저장에 성공하면 `'sow:saved'` 커스텀 이벤트를 쏜다.
3. `reading-map.js`가 그 이벤트를 듣고, 등록된 장들을 `localStorage`의 `sow.read.chapters`(책 id → 읽은 장 배열)에 추가한다.

지도의 각 사각형을 클릭하면 그 장을 "하루한장 코스"로 바로 열어준다(하루한장은 어떤 실제 장이든 걸음 번호로 바로 환산 가능하므로 — 17절, 19절과 동일한 `flatChapterIndex` 계산 재사용).

### 16-3. 💾 저장 — 자동저장과 무엇이 다른가
5절의 `persist.js` 자동저장은 입력이 멈추고 0.5초 뒤에 조용히 저장된다. "저장" 버튼은 **화면에 있는 모든 입력을 그 자리에서 즉시** 저장하고, `"오늘 기록 N개를 저장했어요 💾"` 토스트로 확인해준다. 저장 성공 시 지금 보고 있는 뷰(달력이면 달력, 지도면 지도)를 바로 다시 그려서 반영한다.

### 16-4. ↺ 새로고침 — 그냥 `location.reload()`
화면을 다시 불러온다. 새로고침해도 입력값이 그대로 복원되면(=persist.js가 잘 작동하고 있으면) 사용자가 "저장이 진짜 되는구나"를 스스로 확인할 수 있다.

### 16-5. 어디에 붙였나
성경묵상 탭에 들어가는 즉시 뜬다 — **코스를 아직 안 골랐어도** "어떤 코스로 걸을까요?" 화면 위쪽에 뜨고(`renderTrackSelector` 안에서 먼저 그림), 코스를 고른 뒤 콘텐츠 화면에서도 계속 같은 자리에 뜬다(v5.5부터. 그 전엔 코스를 먼저 골라야만 보였다). 스텝형 코스든, stepless인 자유코스든, generated/yearPlan인 하루한장·1년1독 코스든 상관없이 동일하다. 국어/언어/성경관련 지식 탭에는 아직 안 붙였다.

### 16-5-1. Supabase 연동 관련 알려진 실수 — URL 오타
21절에서 처음 연동할 때, `app/js/supabase-client.js`에 프로젝트 URL을 옮겨적으면서 `hzjfwqawdncdjtezcooa`를 `hzjfwqawdncdjteczcooa`로 잘못 적었다(`c`가 하나 더 들어감). 이 때문에 로그인 시도 시 브라우저가 "Failed to fetch"를 던졌다 — 존재하지 않는 서브도메인으로 요청을 보내려다 실패한 것. **긴 랜덤 프로젝트 참조 문자열은 절대 손으로 옮겨적지 말고, Supabase 대시보드의 복사 버튼(📋)으로만 복사할 것** — 이후 이 규칙을 `SUPABASE_SETUP.md`에도 강조해뒀다.

### 16-6. 한계 — 지금은 로컬 저장뿐
달력과 지도가 보여주는 기록도 결국 이 브라우저의 `localStorage`일 뿐이다. 다른 기기에서 보면 안 보이고, 그룹 멤버끼리 서로의 기록을 보는 것도 아직 안 된다 — 진짜 "우리 그룹이 얼마나 꾸준히 했는지"를 보려면 8절의 Supabase 연동이 필요하다(`GROUP_SHARING_DESIGN.md`, 20절). 지금은 어디까지나 "나 혼자, 이 기기에서"의 기록이다.

---

## 17. 하루한장 코스 — 성경 전체 자동 생성 {#daily-chapter-generated}

**작성일** 2026-08-29

### 17-1. 무엇인가
"하루한장 코스"는 성경 66권 전체(1,189장)를 창세기 1장부터 요한계시록 22장까지, 정경 순서 그대로 하루 한 장씩 걷는 코스. **걸음마다 콘텐츠를 미리 만들어두지 않았다** — 15절의 자유코스와 똑같은 공용 4문항 템플릿(`_prompt-template.json`)을 재사용하고, 걸음 번호(`STEP`)를 그날의 실제 책·장으로 자동 변환한다. 그래서 `status: "planned"`가 아니라 **1189걸음이 전부 처음부터 열려있는 `"active"`** 코스다.

### 17-2. 걸음 번호 → 책/장 자동 매핑
```js
function resolveDailyChapterPassage(library, step){
  const allBooks = [...library.oldTestament.books, ...library.newTestament.books]; // 이미 정경 순서
  let remaining = step;
  for(const b of allBooks){
    if(remaining <= b.chapters) return { book: b, chapter: remaining };
    remaining -= b.chapters;
  }
  const last = allBooks[allBooks.length - 1];
  return { book: last, chapter: last.chapters }; // 계시록 22장에서 멈춤(순환 안 함)
}
```
`content/bible/_library.json`(9절)의 66권 순서와 장 수를 그대로 쓴다 — 별도 매핑 데이터를 새로 안 만들었다. 경계값 검증: 1걸음→창세기 1장, 50걸음(창세기 마지막 장)→다음 51걸음은 출애굽기 1장으로 자연스럽게 넘어감, 929걸음(말라기 4장, 구약 끝)→930걸음은 마태복음 1장, 1189걸음은 계시록 22장(성경 끝, 더는 안 넘어감).

### 17-3. "오늘 시작한 게 기준" — 걸음 1 = 창세기 1장
11절의 "걸음 이전/다음 이동 바"가 그대로 동작한다 — 코스에 처음 들어온 날 걸음 1(창세기 1장)부터 시작해서, "다음 걸음"을 누를 때마다 `STEP`이 1씩 늘고 위 매핑 함수가 자동으로 다음 장을 찾아준다. 콘텐츠가 없어서 막히는 지점이 없으므로(1189장 전부 생성 가능) 어린이 코스(11절 주의사항)처럼 "다음 걸음이 아직 없어서 404" 같은 문제가 발생하지 않는다.

### 17-4. 데이터 위치
```
content/meditation/daily-chapter/meta.json   # totalSteps:1189, generated:true (걸음이동바 경계 계산용)
content/bible/_library.json                  # 66권 순서/장수 — 9절과 동일 데이터 재사용
content/meditation/_prompt-template.json     # 공용 4문항 — 15절과 동일 데이터 재사용
```
트랙 레지스트리에 `"generated": true`가 있으면 `shell.js`가 이 렌더러(`renderMeditationDailyChapter`)로 분기한다. 지금은 성경묵상 전용 개념이라 13절(followsTrackOf)의 국어/언어/지식은 이 코스에서도 콘텐츠가 없어 "준비되지 않았어요" 화면이 뜬다.

---

## 18. 1년1독 코스 — 365일 균등 분배 {#year-plan}

**작성일** 2026-08-29 · **채택 방식** 정경 순서 그대로, 하루 3~4장씩 (아래 18-4 대안 비교 참고)

### 18-1. 무엇인가
성경 66권 전체(1,189장)를 정확히 365일에 나눠 읽는 코스. 17절의 하루한장 코스가 "하루 정확히 1장씩(1189일 소요)"이라면, 이 코스는 "365일 안에 다 끝나도록 하루치 분량 자체를 늘린 버전"이다 — 같은 정경 순서, 같은 매핑 함수(`resolveDailyChapterPassage`)를 그대로 재사용한다.

### 18-2. 365일에 정확히 나누는 계산
```js
const TOTAL_CHAPTERS = 1189, TOTAL_DAYS = 365;
function resolveYearPlanRange(day){
  const start = Math.floor((day-1) * TOTAL_CHAPTERS / TOTAL_DAYS) + 1;
  const end   = Math.floor(day * TOTAL_CHAPTERS / TOTAL_DAYS);
  return { start, end };
}
```
1189 ÷ 365 ≈ 3.26이라 매일 정확히 3장은 아니고, **271일은 3장·94일은 4장**으로 자동 분배된다. 위 공식(누적 비율의 정수부 차이, 흔히 "Bresenham 방식"이라 부르는 계산)을 쓰면 4장짜리 날이 한쪽에 몰리지 않고 1년 내내 고르게 섞인다 — 검증: 1일차 1~3장(3장), 17일차 출애굽기 3~5장(3장), 365일차 계시록 19~22장(4장, 정확히 성경 끝에서 마무리).

### 18-3. 여러 장을 하나로 묶어 표시
그날 몫이 여러 장(예: 3장)이면 `resolveDailyChapterPassage`를 반복 호출해 각 장의 책을 확인하고, **같은 책 안에서 연속된 장이면 "창세기 1~3장"처럼 하나로 묶는다.** 책 경계를 넘는 날(예: 구약 마지막 몇 장 + 신약 첫 장)은 자동으로 "말라기 3~4장 · 마태 1장"처럼 두 덩어리로 나뉜다 — `groupYearPlanPassages()`가 이 묶음 처리를 담당한다. 성경 보기 위젯은 그날 첫 장으로 연다(위젯이 한 번에 한 장만 열 수 있는 구조라서 — 9절).

### 18-4. 왜 "구약/신약 섞어 읽기" 대신 이 방식을 먼저 택했나
전통적인 1년1독 플랜(맥체인 플랜 등)은 하루에 구약 조금 + 신약 조금 + 시편을 동시에 읽도록 설계해서 지루함을 줄인다. 더 "정통적인" 방식이지만, 여러 갈래(구약 트랙/신약 트랙)를 동시에 굴려야 해서 계산이 한 단계 더 복잡하다. 지금 방식(정경 순서 그대로, 분량만 조절)은:
- 17절의 계산 로직을 그대로 재사용해서 구현 비용이 거의 없었고
- 365일 안에 끝나는 걸 수학적으로 보장하며
- 나중에 "섞어 읽기"로 업그레이드하고 싶으면, 이 트랙(`bible-in-a-year`)은 그대로 두고 별도 트랙(예: `bible-in-a-year-interleaved`)을 추가하는 식으로 확장하면 된다 — 등록제 구조(1절 원칙 1) 덕분에 기존 걸 안 건드리고 새 코스를 얹을 수 있다.

### 18-5. 데이터 위치
```
content/meditation/bible-in-a-year/meta.json   # totalSteps:365, yearPlan:true
content/bible/_library.json                    # 66권 순서/장수 — 9절·17절과 동일 데이터 재사용
content/meditation/_prompt-template.json       # 공용 4문항 — 15절·17절과 동일 데이터 재사용
```
트랙 레지스트리에 `"yearPlan": true`가 있으면 `shell.js`가 `renderMeditationYearPlan`으로 분기한다.

---

## 19. 본문 바로 가기 — 구약/신약 탭 공용 컴포넌트 {#chapter-jumper}

**작성일** 2026-08-29 · **v2 개정** 2026-09-02(접이식으로 전환) · **함수** `renderChapterJumper()` in `shell.js`

### 19-1. 무엇인가
책/장을 직접 골라서 이동하는 UI. **구약/신약 탭이 책 선택 드롭다운보다 앞에 온다** — 66권을 한 드롭다운에 다 넣으면(예: "애가"를 찾으려면 39번째 항목까지 스크롤) 찾기 번거로워서, 먼저 구약/신약으로 절반을 걸러내고 그 안에서 책을 고르게 했다.

**v2부터는 기본이 접힌 상태다.** 처음엔 항상 펼쳐진 채로 화면 공간을 꽤 차지했는데("공간을 너무 차지 해" 피드백), 지금은 `🔎 본문 바로 가기 (현재 책 N장)` 이라는 **버튼 한 줄만** 보이고, 눌러야 구약/신약 탭 + 책/장 선택창이 펼쳐진다. 펼친 채로 책이나 장을 바꾸면(자유코스처럼 그 자리에서 다시 그리는 경우) 다시 접힌 상태로 돌아간다 — `renderChapterJumper`가 매번 호출될 때 `expanded`가 함수 스코프 지역변수로 초기화되기 때문에 자연스럽게 그렇게 된다.

세 코스 상단에 붙어있다:
- **자유코스(15절)**: 원래 있던 걸 이 컴포넌트로 교체. 고르면 즉시 그 본문으로 화면이 바뀐다(페이지 이동 없음).
- **하루한장 코스(17절)**: "이 장으로 바로 가기" — 고른 책/장을 걸음 번호로 환산해서(`flatChapterIndex`) 그 걸음으로 이동한다.
- **1년1독 코스(18절)**: 고른 책/장이 포함된 날짜(day)를 역산해서(`yearPlanDayForFlatIndex`) 그 날짜로 이동한다.

**어린이 1~20절 코스에는 없다** — 그 코스의 걸음은 실제 성경 장이 아니라 사람이 미리 나눠둔 구간(예: 요한복음 1:1–18)이라, "레위기 3장으로 이동" 같은 장 단위 개념 자체가 안 맞는다.

### 19-2. 재사용 방식
```js
renderChapterJumper(slotEl, {
  library,                    // content/bible/_library.json
  initialBookId, initialChapter,
  label: '...',                // 코스마다 다른 안내문구
  onChange: (bookId, chapter) => { /* 코스마다 다르게 처리 */ }
});
```
컴포넌트 자체(접힌 버튼 ↔ 구약/신약 탭 + 책/장 select 전환 포함)는 세 코스가 완전히 동일한 코드를 쓰고, **선택했을 때 무엇을 할지(`onChange`)만 코스마다 다르다** — 자유코스는 그 자리에서 다시 그리고, 나머지 둘은 걸음/날짜를 계산해서 페이지 이동만 시킨다.

### 19-3. 장 ↔ 걸음/일차 상호 변환
```js
function flatChapterIndex(library, bookId, chapter){ /* 66권 누적 순번으로 변환 */ }
function yearPlanDayForFlatIndex(flatIndex){ /* 18-2의 날짜 범위 계산을 거꾸로 탐색 */ }
```
17절의 `resolveDailyChapterPassage`(걸음→책/장)와 정확히 반대 방향 변환이다. 검증: 레위기 3장 → 하루한장 93걸음 / 1년1독 29일차.

---

## 20. 우리의 성경읽기 — 그룹 진도판 {#our-bible-reading}

**작성일** 2026-08-29

### 20-1. 무엇인가
그룹(8절) 안의 **"🗺️ 우리의 성경읽기"** 탭. 16절의 "나의 성경읽기"(달력/지도)가 개인용이라면, 이건 그룹용이다 — 멤버들이 각자 어떤 코스로 어디까지 읽었는지 한눈에 보여주는 진도판. "찾은 하나님"(13절, 15절 shareable)이 "내용을 나누는 것"이라면, 이건 "진행 상황을 나누는 것"으로 역할이 나뉜다.

### 20-2. 지금은 정적 미리보기
실제 그룹원의 데이터를 보여주려면 Supabase 연동(`GROUP_SHARING_DESIGN.md`)이 있어야 한다. 그래서 지금은 `content/meditation/elementary/group/_ui-copy.json`의 `overview.sampleMembers`에 예시 데이터(가짜 이름·코스·진도)를 넣어두고, **"실제로 연동되면 이런 모습이에요"**라는 안내와 함께 미리보기 형태로만 보여준다:
```json
"sampleMembers": [
  { "name": "나", "course": "어린이 1~20절 코스", "progress": "3/42걸음" },
  { "name": "OO", "course": "하루한장 코스", "progress": "17/1189장" },
  { "name": "OO", "course": "1년1독 코스", "progress": "5/365일" }
]
```
다른 그룹 관리 탭(⚙️)과 같은 패턴 — 콘텐츠 담당자가 문구/예시를 바꿀 땐 이 JSON만 고치면 되고, `shell.js`의 렌더링 코드는 그대로 재사용된다.

### 20-3. 실제로 연동될 때 필요한 것
- `group_members`별 `현재 활성 트랙 + 마지막으로 저장한 걸음`을 조회하는 쿼리(신규 컬럼 또는 `group_shared_answers`와는 별개 테이블/뷰 필요 — 진도 자체는 "공유"가 아니라 "그룹에 있다는 사실만으로 자동 노출"되는 정보라 `visibility` 개념이 없다. 이 부분은 프라이버시 관점에서 정할 필요가 있다: 진도까지는 그룹원끼리 자동 공개할지, 아니면 이것도 옵트인으로 할지 — 8절의 "이메일 초대는 수락 대기를 거친다"는 원칙과 별개로 논의가 더 필요하다.
- 16-2-2의 성경지도를 그룹용으로 확장한 버전(멤버별 색을 다르게 표시하는 등)도 고려할 만하다 — 지금은 범위 밖.

---

## 21. Supabase 실제 연동 {#supabase-integration}

**작성일** 2026-08-29 · **프로젝트** `seddofword2` (Seoul 리전) · **파일** `app/js/supabase-client.js`, `app/js/auth-widget.js` (신규)

### 21-1. 지금까지 한 것
`SUPABASE_SETUP.md`대로 프로젝트를 만들고 `entries`/`groups`/`group_members`/`group_shared_answers`/`group_blessings` 5개 표 + RLS 정책까지 SQL로 생성 완료. 이제 실제 앱 코드가 이 프로젝트에 연결됐다:

- **`supabase-client.js`**: `supabase-js`(CDN, `https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/...`) 초기화. 프로젝트 URL과 publishable key(예전 이름 `anon public`)를 여기 하드코딩해뒀다 — 이 키는 원래 브라우저에 노출되도록 설계된 키라 안전하다(원칙: `SUPABASE_SETUP.md` 2단계 참고).
- **`auth-widget.js`**: 상단 네비 바로 아래 로그인 바. 이메일 입력 → "로그인 링크 받기" → 매직링크 메일 발송 → 메일의 링크를 누르면 자동 로그인되어 이 페이지로 돌아온다.
- **`persist.js` 확장**: 로그인 상태면, 로컬 저장(기존 방식 그대로 유지)에 더해 Supabase `entries` 표에도 같은 값을 upsert한다. **로그인 안 해도 예전처럼 로컬에만 저장되고 아무것도 깨지지 않는다** — 하이브리드 방식.

### 21-2. 왜 하이브리드인가 (로컬 + 서버 동시 저장)
로그인을 강제하지 않기 위해서다. 로그인 안 한 사용자도 지금까지처럼 바로 묵상을 시작할 수 있고, 나중에 로그인하면 그 시점부터 서버에도 남기 시작한다. (로컬에 이미 쌓인 과거 기록을 로그인 시점에 서버로 한 번에 옮겨주는 마이그레이션은 아직 없음 — 7절 다음 단계 후보.)

### 21-3. entries 표 upsert 키
`persist.js`의 저장 키(`{moduleId}:{trackId}:{book}:{step}:{promptId}`)를 그대로 쪼개서 Supabase의 `entries` 표에 쓴다. `unique (user_id, module, track_id, book, step, prompt_id)` 제약 덕분에, 같은 걸음을 다시 수정해서 저장해도 새 행이 아니라 **기존 행이 갱신**된다(upsert).

### 21-4. 아직 안 된 것 (다음 단계)
- **그룹 관리 탭의 입력창**(그룹 만들기/이메일 초대) — 여전히 `disabled` 상태. `groups`/`group_members` 표에 실제로 insert하는 코드 연결 필요.
- **찾은 하나님 공유** — `shareable` 체크박스 상태는 이제 `entries.shareable` 컬럼까지 올라가지만, 그걸 그룹원들에게 실제로 보여주는(= `group_shared_answers`에 복사하거나 `entries`를 그룹 단위로 조회하는) 로직은 아직 없다.
- **우리의 성경읽기 진도판** — 여전히 정적 예시 데이터(20-2). 실제 그룹원 진도를 보여주려면 21-4의 위 항목들이 먼저 필요하다.
- **과거 로컬 기록 → 서버 마이그레이션** — 로그인 이전에 로컬에만 쌓인 기록을 로그인 시 서버로 올려주는 일회성 동기화.
- **이 문서 작성 환경에서는 실제 연결을 검증하지 못했다** — Claude가 코드를 작성한 컴퓨터(샌드박스)는 보안상 `supabase.co`, CDN 도메인에 접속이 막혀 있어서, 실제로 로그인/저장이 되는지는 사용자님이 직접 브라우저에서 테스트해봐야 확인된다.

### 21-5. basePath 자동 감지 (GitHub Pages 서브경로 대응)

GitHub 저장소에 올린 것만으로는 웹사이트가 되지 않는다 — `DEPLOY.md` 2절대로 GitHub Pages를 켜야 온라인에서 보인다. 이때 기본 주소가 `https://sowbible.github.io/seed-of-word-v2/`처럼 **서브경로**로 뜨는데, 지금까지 코드가 CSS/JS를 전부 `/app/css/...`처럼 **사이트 루트 기준 절대경로**로 불러오고 있었다 — 서브경로 배포에서는 이게 깨진다(주소 앞에 `/seed-of-word-v2`가 안 붙어서 엉뚱한 곳을 찾음).

**해결**: `sow/read/index.html`이 실행되자마자 **자기 자신의 주소(`location.pathname`)에서 `/sow/read/index.html` 앞부분을 잘라내 `basePath`를 스스로 계산**하고, CSS/JS 태그를 `document.write`로 그 basePath를 붙여서 동적으로 만들어낸다:
```js
var SOW_SUFFIX = '/sow/read/index.html';
var SOW_PATH = location.pathname;
window.SOW_BASE_PATH = SOW_PATH.indexOf(SOW_SUFFIX) > -1 ? SOW_PATH.slice(0, SOW_PATH.indexOf(SOW_SUFFIX)) : '';
```
검증(로컬 서버로 실제 서브경로 배포 흉내내서 확인): 루트에서 열면(`/sow/read/index.html`) `basePath = ''`, 서브경로에서 열면(`/seed-of-word-v2/sow/read/index.html`) `basePath = '/seed-of-word-v2'` — 정확히 계산됨.

이제 **로컬 / GitHub Pages 서브경로 / 커스텀 도메인(seedofword.org, 루트) 중 어디에 배포해도 코드를 안 고쳐도 된다.** 저장소 루트에 `index.html`도 새로 추가했다 — `https://seedofword.org/`처럼 루트로 들어와도 자동으로 `sow/read/index.html`(성경묵상 화면)로 이동시켜준다.

---

## 22. 모바일 앱 디자인 {#mobile-app-feel}

**작성일** 2026-09-01

"핸드폰에서 앱처럼 느껴지게" 요청에서 나온 두 가지 변경: **PWA(홈 화면 설치)**와 **하단 탭바**.

### 22-1. PWA — 홈 화면에 설치하면 진짜 앱처럼 열림

`manifest.json`(저장소 루트)을 추가했다. 모바일 브라우저에서 "홈 화면에 추가"를 하면:
- 브라우저 주소창 없이 **전체화면(standalone)**으로 열린다
- 홈 화면에 **전용 아이콘**(새싹 잎사귀 모양, 아래 22-2)이 생긴다
- 상태바 색이 브랜드 색(`--ink`, 짙은 파인그린)에 맞춰진다

```json
{
  "name": "SOW · Seed of Word",
  "short_name": "SOW",
  "start_url": "sow/read/index.html?book=john&step=1&module=meditation",
  "display": "standalone",
  "theme_color": "#1D3A2C",
  "icons": [ ... ]
}
```
`start_url`과 아이콘 경로가 전부 **상대경로**라서, manifest 파일 자신의 위치(사이트 루트) 기준으로 알아서 풀린다 — 12절의 basePath 자동 감지와 마찬가지로 로컬/서브경로/커스텀 도메인 어디서든 따로 손볼 필요가 없다.

`sow/read/index.html`의 `<head>`에서 manifest·아이콘·`theme-color`·iOS 전용 메타태그(`apple-mobile-web-app-capable` 등)를 전부 basePath를 붙여서 동적으로 연결한다(9절과 같은 `document.write` 패턴).

> **아직 안 한 것**: 오프라인 캐싱(서비스 워커)은 이번엔 안 넣었다 — 캐시 무효화 전략까지 같이 설계해야 해서 범위 밖으로 뒀다. 지금은 "홈 화면에서 앱처럼 열리는 것"까지만.

### 22-2. 아이콘 — 공식 로고로 교체(v5.3)

처음엔 Python(Pillow)으로 직접 그린 잎사귀 아이콘을 썼는데, v5.3부터는 **사용자님이 만들어준 공식 SOW 로고**로 전부 교체했다 — 파랑 S · 주황 O(새싹이 돋아나는) · 빨강 W + "Seed of Word" 서브텍스트, 흰 카드 배경.

```
app/icons/
  favicon-16.png / favicon-32.png   # 브라우저 탭 아이콘
  apple-touch-icon.png (180×180)    # iOS 홈 화면
  icon-192.png / icon-512.png       # PWA manifest, 안드로이드 홈 화면
  icon-1024.png                     # 고해상도 필요한 곳(앱스토어 제출 등) 대비
  sow-logo-official.png             # 원본 로고 — README 배너 등에 재사용
```

`manifest.json`과 `sow/read/index.html`의 `<head>`(favicon, apple-touch-icon)를 전부 이 파일들로 다시 연결했다. 저장소 루트 `index.html`(리다이렉트 화면)과 `README.md`에도 로고를 노출해서, 앱 진입 전에도 브랜드가 보이게 했다.

> 참고: 이 로고 이미지들은 이미 흰 배경이 그림 안에 포함돼 있어서(투명 배경이 아님), manifest의 "maskable" purpose는 쓰지 않았다 — 안드로이드가 자체 마스크를 씌우면 이중으로 잘려 보일 수 있어서, `purpose: "any"`만 지정해 원본 그대로 보이게 했다.

### 22-3. 하단 탭바 — 640px 이하에서 자동 전환

데스크톱/태블릿은 지금처럼 상단 탭 그대로다. 모바일 폭(640px 이하)에서만 CSS 미디어쿼리로 상단 탭 네비(`nav.sow-modules`)를 **화면 하단에 고정**시키고, 아이콘을 라벨 위에 배치해서 실제 앱들(카카오톡, 인스타그램 등)의 탭바처럼 보이게 했다.

```css
@media (max-width: 640px){
  nav.sow-modules{ position:fixed; bottom:0; ... }
  nav.sow-modules button{ flex-direction:column; ... }
}
```
- `env(safe-area-inset-bottom)`을 패딩에 더해서, 아이폰의 하단 홈 인디케이터 바 위로 탭바가 안 가려지게 했다.
- 본문(`main.sow-main`)에 하단 여백을 추가로 줘서, 탭바에 콘텐츠 마지막 줄이 가려지지 않게 했다.
- HTML/JS는 전혀 안 바꿨다 — `shell.js`가 그리는 마크업은 그대로고, CSS만 화면 크기에 따라 다르게 배치한다.

### 22-4. 다음에 더 하면 좋을 것
- 서비스 워커로 오프라인에서도 최근 본 걸음은 열리게 하기
- 스플래시 화면(iOS는 `apple-touch-startup-image`, 안드로이드는 manifest의 `background_color`+아이콘 조합으로 자동 생성됨 — 지금 이미 기본은 됨)
- "홈 화면에 추가" 유도 배너 (iOS는 자동 유도가 안 되므로, 처음 방문 시 안내 문구를 띄우는 것도 고려할 만함)

---

## 23. 반응형 네비게이션 — 드로어(모바일) / 사이드바(데스크톱) 통합 {#responsive-layout}

**작성일** 2026-09-02 · **v2 개정** 2026-09-02(하단 탭바 → 드로어 메뉴로 전환)

### 23-1. 무엇이 문제였나
처음엔(v5.6 이전) 화면 크기와 무관하게 **본문이 항상 가운데 정렬된 760px짜리 좁은 기둥 하나**였다. 넓은 데스크톱 모니터에서는 좌우로 텅 빈 여백만 잔뜩 남고, 실제 콘텐츠는 화면 가운데 얇은 띠처럼 보였다. v5.6에서 데스크톱 사이드바를 만들어 이건 해결했지만, 그때는 모바일이 여전히 "하단 탭바"였고 로그인·달력·그룹 같은 항목을 넣을 자리가 없었다 — 사용자가 참고로 준 앱 이미지(햄버거 → 드로어 메뉴, 로그인/설정을 메뉴 안에)를 보고 v5.7에서 구조를 통합했다.

### 23-2. 지금 구조 — nav 하나가 두 가지 모습으로 렌더링됨
`shell.js`의 `renderNav()`가 그리는 목록(4개 모듈 → 구분선 → "📅 나의 성경읽기" → "👥 우리 그룹" → 로그인 위젯)은 **화면 크기와 무관하게 완전히 동일**하다. 다른 건 이 목록을 담은 `<nav id="sow-nav">`를 CSS가 어떻게 배치하느냐뿐이다:

| 화면 | `nav`의 모습 | 여는 방법 |
|---|---|---|
| 1023px 이하 (모바일·태블릿) | 화면 왼쪽 밖에 숨어있는 **드로어**(off-canvas) | 상단 앱바의 햄버거(☰) 버튼 |
| 1024px 이상 (데스크톱) | 항상 보이는 **왼쪽 고정 사이드바** | 필요 없음(항상 보임) |

```css
/* 기본값(1023px 이하) = 드로어. transform으로 화면 밖에 숨겨둔다 */
nav.sow-modules{ position:fixed; left:0; width:82%; max-width:320px;
  transform:translateX(-100%); transition:transform .25s ease; }
body.sow-drawer-open nav.sow-modules{ transform:translateX(0); }

/* 1024px 이상 = 사이드바로 승격, 항상 보이게 transform 해제 */
@media (min-width:1024px){
  nav.sow-modules{ width:var(--sidebar-w); transform:none; transition:none; }
  main.sow-main{ margin-left:var(--sidebar-w); max-width:900px; }
}
```

### 23-3. 모바일 상단 앱바 + 드로어 오버레이
1023px 이하에서만 보이는 `.sow-topbar`(햄버거 + "SOW" 로고)가 새로 생겼다. 햄버거를 누르면 `document.body`에 `sow-drawer-open` 클래스가 붙고, CSS가 그 상태를 보고 nav를 슬라이드인시킨다. 동시에 `.sow-drawer-overlay`(화면 전체를 덮는 반투명 배경)가 나타나서, **바깥을 탭하면 드로어가 닫힌다.** ESC 키로도 닫힌다. 데스크톱(1024px+)에서는 `.sow-topbar`와 `.sow-drawer-overlay` 둘 다 `display:none`으로 완전히 숨겨진다 — 사이드바가 항상 보이니 열고 닫을 필요 자체가 없다.

### 23-4. 로그인·그룹이 메뉴 안에 통합된 방식
전에는 로그인 위젯(`#sow-auth`)이 `nav`와 별개의 `<div>`로 body에 따로 있었다. v5.7부터는 **`renderNav()`가 매번 nav 안에 로그인 위젯 컨테이너를 직접 만들어서 그 안에 렌더링한다** — 그래서 nav가 드로어로 접히든 사이드바로 펼쳐지든, 로그인 폼이 항상 같이 움직인다. "📅 나의 성경읽기"와 "🗺️ 우리의 성경읽기" 버튼은 실제 모듈이 아니라 **바로가기**다 — 전자는 `goTo('meditation', 'myReading')`, 후자는 `goTo('meditation', 'overview')`를 호출한다(24절에서 "나의 성경읽기"가 어떻게 독립 화면이 됐는지 자세히 다룬다).

### 23-5. 타이포그래피 — Noto Serif KR 완전히 제거
헤딩(`.serif` 유틸리티 클래스, 코스 카드 제목, "나의 성경읽기" 제목, 한자 카드, "SOW" 워드마크)에 쓰던 세리프체를 전부 없애고 **Pretendard 산세리프 하나로 통일**했다 — Claude 자체 웹/앱 인터페이스가 세리프 없이 깔끔한 산세리프 하나로 일관되게 가는 것을 참고했다. `.serif` 클래스 이름 자체는 그대로 남겨뒀다(이미 여러 군데 HTML/JS 템플릿에서 `class="... serif"`로 쓰고 있어서, 이름을 바꾸면 그 사용처를 다 찾아 고쳐야 하는데 그럴 필요 없이 **CSS 정의만 바꾸면 되기 때문**이다). `index.html`의 Google Fonts 링크에서도 `Noto+Serif+KR` 임포트를 제거해서 폰트 로딩 자체가 가벼워졌다.

### 23-6. 왜 이렇게 구현했나
- **하나의 nav = 하나의 소스** — 모듈 목록, 바로가기, 로그인을 데스크톱/모바일용으로 각각 따로 만들지 않고 `renderNav()` 하나가 둘 다 렌더링한다. 새 메뉴 항목이 하나 늘어나도 한 군데만 고치면 웹/앱 모두에 반영된다.
- **참고 이미지(Zero ERP 앱)의 핵심 패턴 채택** — 햄버거→드로어, 드로어 뒤 어두운 오버레이, 안의 항목은 아이콘+글자 가로 배치. 다만 참고 이미지의 프로필 아바타 팝업(이름/조직/설정/로그아웃)은 지금 앱 상태(이메일 로그인/로그아웃 정도)에 비해 과했다고 판단해 그대로 베끼지 않고, 로그인 위젯을 메뉴 안에 자연스럽게 포함시키는 더 단순한 형태로 반영했다.

---

## 24. 코스 아코디언 & "나의 성경읽기" 전용 화면 {#nav-accordion-my-reading}

**작성일** 2026-09-02

### 24-1. 메뉴 안에서 바로 코스 고르기 (아코디언)
`hasTracks: true`인 모듈(지금은 성경묵상뿐)은 메뉴에서 이름 옆에 화살표(▸/▾)가 붙는다. 눌러도 화면이 안 바뀌고, **그 자리에서 바로 아래로 코스 목록이 펼쳐진다**:
```js
async function renderCourseSublist(sublistEl, moduleId){
  const tracks = await getTrackRegistry(moduleId);
  // 지금 고른 코스에는 .active 표시, 코스를 누르면 그 코스로 바로 이동(goTo)
}
```
지금 활성화된 모듈이면 처음부터 펼쳐진 채로 시작한다. 예전엔(10절) 코스를 바꾸려면 "↺ 코스 변경" 버튼을 눌러 본문 영역에 코스 카드 화면을 다시 띄워야 했는데, 이제 메뉴에서 화살표만 눌러 바로 다른 코스로 갈아탈 수 있다 — 다만 처음 진입 시 코스를 하나도 안 고른 상태의 "코스 선택 화면"(10절)은 그대로 남아있다. 둘은 공존한다: 처음 안내엔 큰 카드로, 이후 전환엔 메뉴에서 빠르게.

### 24-2. "나의 성경읽기"는 완전히 독립된 화면이다
처음엔(v5.5) "나의 성경읽기"(달력·성경지도)를 성경묵상의 모든 화면(코스 선택 화면이든 콘텐츠 화면이든) 맨 위에 항상 끼워 넣었다. 이러면 "언제 봐도 보인다"는 장점은 있지만, **코스 카드나 묵상 질문 같은 다른 내용과 뒤섞여서 "이것만 보는 화면"이 아니게 된다**는 문제가 있었다("이걸 볼 수 있는 화면이 있었으면 좋겠는데" 피드백). 그래서 v6.0부터는:
- 코스 선택 화면과 코스 콘텐츠 화면에서 인라인 렌더링을 **없앴다.**
- 대신 메뉴에 "📅 나의 성경읽기" 항목을 (다시) 만들고, 누르면 `state.activeSub.meditation = 'myReading'`을 거쳐 **코스/트랙 로직을 아예 안 타는** 전용 화면으로 간다:
```js
if(moduleId === 'meditation' && state.activeSub.meditation === 'myReading'){
  main.innerHTML = '';
  window.SOWSessionToolbar?.render(main);
  return; // 트랙 해석, 코스 카드, 묵상 질문 전부 건너뜀
}
```
- "성경묵상" 메인 버튼이나 코스를 새로 고를 때는 `activeSub`를 `'steps'`로 명시적으로 되돌린다 — 안 그러면 "나의 성경읽기" 화면에 갇힌 채로 성경묵상을 눌러도 계속 그 화면만 보이는 버그가 생긴다.

### 24-3. 오늘의 활동 요약 — 참고 이미지(TellmeNow 대시보드) 반영
사용자가 참고로 준 B2B ERP 대시보드(인사말 + 색깔 있는 통계 카드 줄 + 상세 내용)를 보고, "나의 성경읽기" 화면 맨 위에 같은 구조를 얹었다:
```
📖 나의 성경읽기
오늘도 씨앗 하나 심으러 오셨네요 🌱
[✅/⏳ 오늘 기록] [🔥 연속 기록일] [🗓️ 이번 달 기록] [📖 전체 읽은 장]
📅 달력 / 🗺️ 성경지도  ← 기존 상세 내용
```
네 가지 통계는 전부 이미 있던 로컬 데이터(`sow.activity.dates`, `sow.read.chapters`)에서 그 자리에서 계산한다 — 별도 저장소나 서버 조회가 필요 없다:
- **오늘 기록**: 오늘 날짜가 활동 로그에 있는지
- **연속 기록일**: 오늘(또는 어제)부터 거꾸로 며칠 연속 기록이 있는지 세기
- **이번 달 기록일수**: 이번 달에 해당하는 날짜 키 개수
- **전체 읽은 장**: 성경지도 데이터(`sow.read.chapters`)의 책별 배열 길이를 다 더한 값

색은 참고 이미지의 파랑/주황/빨강 대신 **우리 팔레트**(`--sprout`, `--amber`, `--clay`)로 바꿔서 톤을 맞췄다. "💾 저장" 버튼을 누르면 이 통계도 즉시 다시 계산해서 갱신된다.


---

## 25. 국어 — 어휘 / 한자 / 나눔(글쓰기) {#korean-3-tabs}

**작성일** 2026-09-03

### 25-1. 무엇이 바뀌었나
예전엔 국어 서브모듈이 "어휘·한자"(한 파일, 한 탭에 합쳐짐) + "토론"(빈 틀) + "글쓰기"(빈 틀), 이렇게 3개였다. "토론"과 "글쓰기"는 콘텐츠가 하나도 안 채워진 스캐폴드 상태로 오래 남아있었다. v6.2부터 **어휘 / 한자 / 나눔(글쓰기)** 3개로 재구성했다:
- **어휘** — 그날 본문과 어울리는 낱말 5개(아이콘+뜻)
- **한자** — 낱말 5개와 별개로, 한자 1자를 훈음·의미태그·설명·성경과의 연결까지 카드 하나로
- **나눔(글쓰기)** — 그날 배운 어휘·한자를 넣어서 하나님께 하고 싶은 말을 자유롭게 쓰는 질문 1개(실제 콘텐츠로 채워짐 — 예전 "토론"은 이 안으로 통합되며 삭제됐다)

### 25-2. 데이터 위치
```
content/korean/elementary/vocab/john/{n}.json    # { relatedWords: [...] }
content/korean/elementary/hanja/john/{n}.json    # { hanja: {...} }
content/korean/elementary/writing/john/{n}.json  # { writingPrompt: {...} }
```
셋 다 독립된 파일이라, 어휘만 먼저 채우고 한자·나눔은 나중에 채워도 된다 — 안 채운 탭은 "아직 콘텐츠가 채워지지 않았어요"로 자연스럽게 표시된다(원칙 3, "빈 슬롯은 자연스럽게").

### 25-3. 렌더러 3개로 분리
`shell.js`에 `renderKoreanVocab`(어휘 목록만) / `renderKoreanHanja`(한자 카드만) / `renderKoreanWriting`(나눔 프롬프트)를 각각 만들었다 — 예전엔 `renderKoreanVocab` 하나가 한자+어휘+표현하기를 다 그렸는데, 지금은 탭 하나당 함수 하나로 깔끔하게 나뉜다. `SUB_RENDERERS.korean`에 `{ vocab, hanja, writing }`로 등록돼 있고, `_registry.json`의 순서(어휘→한자→나눔)를 그대로 따라간다.


---

## 26. 국어/언어/성경관련 지식 — 트랙 무관 구조로 재설계 {#track-independent-content}

**작성일** 2026-09-03 · **적용 버전** v7.0

### 26-1. 어떤 버그였나
국어/언어/성경관련 지식은 `followsTrackOf: "meditation"`으로, 성경묵상이 지금 고른 코스를 그대로 따라간다(13절). 그런데 v6.2까지는 콘텐츠 파일 자체가 `content/korean/elementary/vocab/john/1.json`처럼 **트랙 이름(`elementary`)이 경로에 박혀있었다.** 성경묵상에서 하루한장 코스나 1년1독 코스를 고르면, 국어는 `content/korean/daily-chapter/...` 같은 존재하지도 않는 경로를 찾으려다 실패했다 — 즉 **자유코스/하루한장/1년1독 코스에서는 국어 탭이 원천적으로 동작할 수 없는 상태**였다.

### 26-2. 해결 — 콘텐츠를 "실제 책/장" 기준으로
국어 콘텐츠(한자, 어휘, 나눔)는 애초에 **어떤 코스로 왔는지와 무관하게, 그 성경 구절 자체에 대한 것**이다("言"이라는 한자는 요한복음 1장이면 늘 "言"이지, 코스에 따라 달라지지 않는다). 그래서 트랙 레이어를 아예 없앴다:
```
# 예전 (트랙 종속)
content/korean/elementary/vocab/john/1.json

# 지금 (실제 책/장 기준, 트랙 무관)
content/korean/vocab/john/1.json
```
`_registry.json`(서브탭 목록)도 트랙별로 안 나뉘고 `content/korean/_registry.json` 하나로 통합됐다.

### 26-3. `resolveRealChapter()` — 코스가 뭐든 "지금 실제 책/장"을 계산
문제는 트랙마다 "지금 어디를 읽고 있는지"를 표현하는 방식이 다 다르다는 것이다:
- **어린이 코스**: 걸음 번호(STEP)가 실제 장과 다르다(여러 걸음이 같은 장에 속할 수 있음) → 걸음 콘텐츠 JSON 안의 `book`/`chapter` 필드를 읽어야 한다.
- **자유코스**: 사용자가 직접 고른 책/장이 `localStorage`(`sow.free.passage`)에 있다.
- **하루한장**: STEP이 곧 성경 전체 누적 장 번호라, 17절의 `resolveDailyChapterPassage()`로 계산한다.
- **1년1독**: STEP이 "며칠째"라, 18절의 `resolveYearPlanRange()` + `groupYearPlanPassages()`로 그날 첫 장을 계산한다.

```js
async function resolveRealChapter(trackId){
  if(trackId === 'elementary'){ /* 걸음 JSON의 book/chapter 필드 사용 */ }
  if(trackId === 'free'){ /* localStorage의 sow.free.passage 사용 */ }
  if(trackId === 'bible-in-a-year'){ /* resolveYearPlanRange + groupYearPlanPassages */ }
  /* daily-chapter(기본값): resolveDailyChapterPassage */
}
```
이 함수 하나가 트랙 종류를 흡수해줘서, 국어/언어/지식 렌더러는 트랙 얘기를 몰라도 된다 — `renderKoreanVocab(container, real)`처럼 그냥 `{ book, chapter }`만 받아서 쓴다.

### 26-4. 콘텐츠가 없는 장으로 가면 자연스럽게 "준비중"
예를 들어 하루한장 코스로 출애굽기 1장까지 가면, `content/korean/vocab/exodus/1.json`이 아직 없으니 fetch가 404가 난다. `renderModulePanel`의 try/catch가 이걸 잡아서 원래부터 있던 "아직 콘텐츠가 채워지지 않았어요" 화면(원칙 3)으로 자연스럽게 넘어간다 — 새로 뭔가를 만들 필요 없이, 기존 안전장치가 그대로 작동한다.

### 26-5. 메뉴에도 아코디언 — `renderSubmoduleSublist()`
15-1절(코스 아코디언)과 같은 원리로, 국어/언어/성경관련 지식도 메뉴에서 화살표를 누르면 그 모듈의 서브탭(어휘/한자/나눔 등)이 메뉴 안에 바로 펼쳐진다. `renderNav()`가 모듈마다 `m.hasTracks`(성경묵상 → 코스 목록)인지 `m.followsTrackOf`(국어 등 → 서브탭 목록)인지로 분기해서 같은 아코디언 UI를 재사용한다.

### 26-6. 타이포그래피 통일
국어의 어휘 카드(`.sow-word-chip`)와 한자 카드(`.sow-hanja-*`) 글자 크기를 v6.1에서 이미 키운 성경묵상 크기(질문 제목 17px, 안내문구 15px)에 맞춰 같이 키웠다 — 국어만 유독 작게 보이던 걸 바로잡았다.


---

## 27. 어휘 콘텐츠 원칙 & 낱말 퀴즈 {#vocab-principles-quiz}

**작성일** 2026-09-05

### 27-1. 어휘 선정 두 가지 기준
1장 예시를 다시 검토하면서 어휘 선정 기준을 확정했다:
1. **그 장 본문에 실제로 나온 단어일 것** — 같은 한자가 들어간 아무 단어(예: "말씀 언"이 들어간 "국어", "언어")가 아니라, 그 구절에 실제로 쓰인 단어여야 한다.
2. **아이들이 일상에서 이미 쓰는 쉬운 말이 아닐 것** — "빛", "어둠", "말씀"처럼 본문에서 특별한 뜻으로 쓰였어도, 단어 자체가 흔하면 제외한다. 두 기준을 **둘 다** 넘어야 어휘로 고른다.

1장 최종 예시(요한복음 1:1–18): 태초(1절) · 독생자(14절) · 권세(12절) · 은혜(16절) · 진리(17절) — 전부 본문에 실제로 나오면서, 초등학생에게는 설명이 필요한 말이다.

**한자 카드와 어휘 목록이 겹칠 필요는 없다** — 한자 카드는 "낯선 한자로 단어의 뿌리를 보여주는 것"이 목적이고, 어휘 목록은 "낯선 낱말 배우기"가 목적이라 역할이 다르다(예: 한자 카드는 言="말씀 언"을 다루지만, "말씀"은 너무 흔해서 어휘 목록에는 없다).

### 27-2. 낱말 퀴즈 — 새 콘텐츠 없이 오늘의 어휘를 재활용
"배움과 어떻게 연결할까"라는 질문에서 나왔다. **새 콘텐츠를 하나도 안 만들고**, 오늘의 어휘 5개(`relatedWords`)를 그대로 재활용해서 3지선다 퀴즈를 그 자리에서 만든다:
```js
const distractors = shuffle(words.filter(w => w.word !== correct.word)).slice(0, 2);
const options = shuffle([correct, ...distractors]);
```
정답 1개(오늘 배운 단어) + 같은 날 다른 단어 중 무작위 2개를 오답으로 섞는다 — 그래서 콘텐츠 작성자가 퀴즈 문제를 따로 안 만들어도, 어휘 5개만 채우면 퀴즈가 자동으로 딸려온다.

### 27-3. 왜 "가볍게" — 점수 없음, 선택사항
- 어휘 탭 맨 아래 "🎯 낱말 퀴즈로 복습하기" 버튼 하나로만 존재한다 — 안 눌러도 되고, 눌러야만 나타난다.
- 맞았는지 틀렸는지 그 자리에서 색으로 바로 보여주고(정답=초록, 오답=클레이색), **점수나 등수는 어디에도 안 보여준다.** 5문제 다 풀면 "🌱 오늘 어휘 N개 다 풀어봤어요!"로 담백하게 끝난다.
- 이건 1절 원칙 5("비교보다 기록")를 그대로 따른 것이다 — 그룹 화면에서 순위를 안 보여주는 것과 같은 이유로, 개인 퀴즈에서도 "몇 점 받았는지"보다 "오늘 이만큼 복습했다"는 사실만 남긴다.

### 27-4. 재사용 가능성
`renderVocabQuizToggle()` / `runVocabQuiz()`는 `{word, shortDesc}` 배열만 받으면 동작하는 범용 함수라, 나중에 다른 모듈(예: 성경관련 지식의 인물/지도 항목)에도 그대로 갖다 쓸 수 있다 — 특정 콘텐츠 스키마에 종속되지 않았다.
