# 지금 바로 미리보기 — Windows 따라하기 (클릭 단위)

지금 다운로드한 `sow-app-v3.1.zip`을 실제로 눈으로 보는 방법이에요. 순서대로 그대로 따라 하시면 돼요.

---

## 1단계. 압축 풀기

1. 다운로드 폴더에서 `sow-app-v3.1.zip` 파일을 찾는다
2. 파일 위에서 **마우스 오른쪽 클릭 → "압축 풀기"(또는 "Extract All...")** 클릭
3. 나오는 창에서 그냥 **"압축 풀기"** 버튼 클릭 (경로 기본값 그대로 둬도 됨)
4. `sow-app-v3.1` 이라는 이름의 폴더가 새로 생긴다 (스크린샷에서 보신 것처럼 `sow-app-v3.1_1`처럼 숫자가 붙을 수도 있어요 — 같은 이름 폴더가 이미 있을 때 Windows가 자동으로 붙이는 거라 문제 없어요)

## 2단계. 그 폴더 안에서 PowerShell 열기

**중요**: PowerShell을 아무 데서나 열면 안 되고, 방금 압축 푼 그 폴더 "안에서" 열어야 해요.

1. 방금 생긴 `sow-app-v3.1` (또는 `sow-app-v3.1_1`) 폴더를 **더블클릭해서 그 안으로 들어간다**
2. 폴더 창 안에 `SOW_구조설계.md`, `README.md`, `content`, `app`, `sow` 같은 파일/폴더들이 보이는 상태여야 함
3. 그 상태에서 폴더 창 위쪽의 **주소 표시줄(경로가 써있는 곳)을 클릭**해서 지우고, `powershell` 이라고 입력한 뒤 **Enter**

   (주소 표시줄에 직접 타이핑하는 방식이 제일 쉬워요. 안 되면: 폴더 안 빈 곳에서 `Shift + 마우스 오른쪽 클릭` → "여기서 PowerShell 창 열기" 선택)
4. 파란색/검은색 PowerShell 창이 뜨는데, 맨 위 줄에 **`sow-app-v3.1` 폴더 경로가 찍혀 있는지 꼭 확인**하세요 (이게 잘못되면 다음 명령어가 안 먹혀요)

## 3단계. 서버 실행 명령어 입력

> 지금 `py -m http.server 8000`을 입력했는데 빨간 글씨로 `'py' 용어가... 인식되지 않습니다`가 떴다면, 컴퓨터에 파이썬이 아예 설치돼 있지 않은 거예요. **맨 아래 "문제 해결" 항목을 먼저 하고 오세요** — 설치 후 여기로 돌아오시면 됩니다.

PowerShell 창에 아래를 **그대로 타이핑**하고 Enter:

```
py -m http.server 8000
```

**성공하면 이렇게 보여요** (그리고 커서가 깜빡이며 그대로 멈춰있어요 — 이게 정상이에요, 서버가 계속 켜져있는 중이라는 뜻):
```
Serving HTTP on :: port 8000 (http://[::]:8000/) ...
```

**만약 `py`가 안 먹히면(에러 나면)**, 대신 이걸 입력해보세요:
```
python -m http.server 8000
```

**이것도 안 되면** → 컴퓨터에 파이썬이 제대로 설치가 안 된 상태예요. 아래 "문제 해결" 참고.

> ⚠️ 이전에 `python3 -m http.server 8000`을 입력하셨을 때, 화면에 그냥 "Python"이라고만 뜨고 바로 다시 입력할 수 있는 상태로 돌아왔었죠? 그건 서버가 실행된 게 아니라, Windows에 있는 가짜 `python3`(Microsoft Store로 연결만 해주는 껍데기)가 실행된 거예요. `py` 또는 `python`으로 다시 시도하면 돼요.

## 4단계. 브라우저에서 열기

**"브라우저"**란 평소 인터넷 검색할 때 쓰는 프로그램이에요 — 크롬(Chrome), 엣지(Edge), 웨일(Whale) 같은 거요. 바탕화면이나 작업표시줄(화면 맨 아래)에서 그 아이콘을 찾아 클릭하세요.

1. 3단계에서 연 PowerShell 창은 **닫지 말고 그대로 켜둔 채로** 둔다
2. 브라우저(크롬 등) 아이콘을 클릭해서 **새로 하나 연다** — PowerShell 창과는 완전히 별개의 프로그램이에요, 둘 다 켜져 있어야 해요
3. 브라우저 창 맨 위에 인터넷 주소를 입력하는 칸(주소창)이 있어요 — 평소 "네이버"라고 검색하시던 그 칸
4. 그 주소창을 클릭해서 기존 내용을 지우고, 아래 주소를 **정확히 그대로** 입력한 뒤 Enter:
   ```
   http://localhost:8000/sow/read/index.html?book=john&step=1
   ```
   (검색하듯이 입력하면 안 되고, 이 전체를 주소로 입력해야 해요)

여기서 **요한복음 1장 묵상 페이지가 보이면 성공**이에요! 상단 탭(성경묵상/국어/언어/성경관련 지식)을 눌러가며 확인해보세요.

## 5단계. 끝나면 서버 끄기

다 보셨으면, 아까 그 PowerShell 창을 클릭하고 **`Ctrl + C`** 를 누르면 서버가 꺼져요. 그냥 창을 닫아도 꺼져요.

---

## 문제 해결

**`py`도 `python`도 둘 다 에러가 날 때(`CommandNotFoundException` 등)**: 컴퓨터에 파이썬이 설치돼 있지 않은 거예요. 아래 순서대로:

1. https://www.python.org/downloads/ 접속 → 노란색 **"Download Python 3.x.x"** 버튼 클릭 (자동으로 Windows용 설치 파일이 받아져요)
2. 다운로드된 설치 파일(`python-3.x.x-amd64.exe`)을 **더블클릭해서 실행**
3. 설치 시작 화면 **맨 아래**에 있는 체크박스 **"Add python.exe to PATH"에 반드시 체크** ← 이거 안 하면 또 안 됩니다. 화면 첫 줄에 있어요, 놓치기 쉬우니 꼭 확인
4. **"Install Now"** 클릭 → 설치 끝날 때까지 기다림 (1~2분)
5. 설치가 끝나면 "Disable path length limit" 같은 추가 화면이 나올 수 있는데, 그냥 닫아도 됩니다
6. **지금 열려있는 PowerShell 창을 완전히 닫는다** (매우 중요 — PATH가 갱신되려면 새 창이어야 해요)
7. `sow-app-v3.1_2` 폴더로 다시 들어가서 2단계(주소창에 `powershell` 입력)부터 다시 진행
8. 새 PowerShell 창에서 먼저 아래로 설치 확인:
   ```
   python --version
   ```
   `Python 3.x.x` 같은 버전이 나오면 성공. 그 다음 3단계의 `python -m http.server 8000`(이번엔 `py` 대신 `python`)으로 진행하세요.

**페이지는 열리는데 아무 내용도 안 보이거나 계속 "불러오는 중…"만 뜰 때**:
- 주소가 정확히 `http://localhost:8000/sow/read/index.html?book=john&step=1`인지 확인 (물음표 뒤 `?book=john&step=1`까지 정확히)
- PowerShell 창이 중간에 꺼지진 않았는지 확인 (꺼졌으면 2~3단계부터 다시)

**"이 사이트에 연결할 수 없음"이 뜰 때**:
- PowerShell에서 `py -m http.server 8000` 실행한 그 창이 지금도 켜져 있는지 확인
- PowerShell 창 맨 위 경로가 `sow-app-v3.1` 폴더가 맞는지 확인 (엉뚱한 폴더에서 실행했으면 안 됨)
